extends RefCounted

# Keep settlement data and replay services intact; suspend its product entry/UI.
const SETTLEMENT_ENABLED:=false

# Town life is suspended together with settlement product entry.
const TOWN_ENABLED:=false
