# SRPG-INDIVIDUAL-TURN-20260915 protocol

사용자가 2026-09-15 이 세션의 직접 구현을 승인했다. 기준 코드는 `8254624`, 요청 문서는 `d9dd319`이다.

## 구현 계약

- 방 기반 전투에 개별 턴을 적용한다. 배치는 기존 무시간 단계로 유지한다. 적 선이동과 진영 우선 정렬은 제거한다. 기존 initiative 내림차순/ID 동률 순서를 사용한다.
- 현재 아군만 행동을 편집한다. 진행은 현재 아군의 지시를 실행하고, 차례가 끝났으면 다음 아군까지 적의 차례를 순서대로 실행한다. 라운드 시작이 적이면 진행으로 그 적 차례를 처리한다. 미래 적 계획은 공개 공격 예고로 취급하지 않고 실제 차례에 최신 상태에서 결정한다.
- 이동 기본 2칸, `clamp(floor(2 * 이동속도/100), 1, 3)`을 부상/이동 지연 반영 후 1~3칸으로 제한한다. 적의 역할별 기본 이동도 1~3칸으로 제한한다. 이동 예산은 한 차례 내 공유하며 이동 확정 뒤에도 공격할 수 있다.
- 기본 공격 횟수는 `clamp(floor(100 / 속도 보정된 무기 공격+재장전 시간), 1, 3)`이다. 무기 없는 공격은 100시간 기준이다. 팔 부상의 기존 피해 보정은 유지한다. 공격은 한 번씩 지정·확정하며 남은 횟수 안에서 대상 변경이 가능하다. 스킬·아이템은 차례를 마친다. 빈 대기는 남은 예산을 포기하고 차례를 마친다. 마지막 공격을 쓰면 자동으로 다음 차례로 간다.
- 개별 행동 중에는 world_time을 증가시키지 않는다. 전체 순서 완료 시 기존 100시간 경계 처리를 한 번 적용한다. 라운드 동안 cooldown은 개별 공격 횟수와 중복 적용하지 않는다. 턴 종료 시 준비 시간을 라운드 경계에 맞춘다.
- 증원은 7라운드마다 2마리, 기존 상한/안전 배치 계약을 유지한다. 도착한 증원은 다음 라운드에 참여한다.
- 저장 상태에 공격 소모 원장을 추가하고 generator 버전을 갱신한다. 저장을 삭제하지 않는다. 다른 기존 실험용 비방 전투의 일괄 계획 방식은 보존한다.
- 던전 기둥은 기존 승인 terrain atlas의 column 4 기둥 sprite를 사용한다. 바닥을 올린 낮은 육면체 표현을 교체한다. 좌표·충돌은 보존한다.

## 검증 및 종료

Godot에서 개별 순서·이동/공격 예산·중복 revision·사망/대기·저장/재생·7라운드 증원을 검사한다. 실제 360px UI에서 현재 차례와 입력/화면 기둥을 확인한다. 첫층 초기 적 1마리 및 기존 방 생성/비방 전투 회귀를 실행한다. 검증한 결과와 한계를 결과 문서에 기록하고 관련 파일만 커밋한다. 추가 난이도 정책은 실행하지 않는다.
