# 도감 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 원정을 넘어 쌓이는 도감(몬스터·영혼석·장비 탭)을 `user://codex.json`에 기록·저장하고 화면으로 보여 준다.

**Architecture:** 정적 모듈 `expedition/progression/codex.gd`가 파일 읽기·쓰기, 사건 기록(`note_*`), 화면용 항목(`monster_entry`/`stone_entry`), 완성률을 맡는다. 세션은 읽어 둔 사전 `s.codex`와 기록 여부 `s.records_codex`(메인 화면의 "새 탐험"만 참)를 가진다. 기록은 사전에만 하고 `flush`가 모아서 파일에 쓴다. 화면은 `expedition/ui/screens/codex_screen.gd` 하나가 모달 팝업(`ui.details_popup`)으로 그린다.

**Tech Stack:** Godot 4 GDScript. 테스트는 `extends SceneTree` 스크립트(`godot --headless --path . --script res://tests/<name>.gd`), 실패 시 종료 코드 1과 `ERROR` 로그.

**Spec:** `docs/superpowers/specs/2026-09-26-codex-design.md`

## Global Constraints

- 파일 경로 기본값 `user://codex.json`, 테스트는 `Codex.path`를 `user://test_codex_<suite>.json`으로 바꾸고 끝에 지운다.
- 파일 형식: `{"version":1,"runs":int,"deepest":int,"monsters":{},"stones":{},"unrands":{},"tips":{}}`. 모르는 최상위 키는 읽고 다시 쓸 때 그대로 둔다.
- 깨진 파일(JSON 사전이 아님)은 `user://codex.bad.json`으로 옮기고 빈 도감으로 시작한다. 이미 `.bad.json`이 있으면 덮어쓴다(최근 것 하나만 보관).
- 몬스터 키: 보스면 `"boss:<boss_kind>"`, 아니면 `species_id`. 영혼석 키: `Essences.canonical(id)`에서 `@속성`을 뗀 것(보스 영혼석은 맨 id).
- 기록은 `s.records_codex == true`일 때만. 기본값 `false`. `start_screen.gd`의 `new_run`만 `true`로 켠다. 아레나·시뮬레이터·난이도 게이트·테스트 세션·`grant_test_loadout`으로 넣은 영혼석은 기록하지 않는다.
- 쓰기 시점: 층 이동(`Descent.descend`), 야영 진입(`camp.gd`의 `phase = "CAMP"`), 원정 끝(VICTORY/DEFEAT 결과 카드), 앱 일시정지·종료 알림(`main.gd`의 `_notification`). 그 밖에는 쓰지 않는다.
- 장비 탭은 고정 아티팩트 목록(`data/content/unrands.json`)이 없으면 숨긴다.
- **작업 트리에 다른 진행 중 변경이 있을 수 있다.** 커밋은 각 Task의 파일만 `git add <경로>`로. `git add -A`/`.` 금지. 같은 파일에 남의 변경이 섞였으면 `git add -p`로 이 Task 변경만 고른다. 판단이 어려우면 멈추고 묻는다.

## Review Focus

- **웹 빌드와 첫 실행**: `user://` 폴더에 파일이 없을 때 빈 도감으로 열리고, 첫 `flush`가 파일을 만든다. Task 1 테스트.
- **테스트·시뮬레이터가 실제 도감 파일을 만들거나 바꾸지 않음**: 기록 꺼짐 경로에서 `flush`가 파일을 건드리지 않는다. Task 1·2 테스트(`user://codex.json`의 수정 시각/존재 비교).
- **같은 몬스터를 여러 번 봄**: 시야가 매 행동 갱신돼도 `seen` 기록은 새로 본 경우에만 바뀌고 `dirty`가 계속 켜지지 않는다. Task 2 테스트.
- **변종·보스 키**: 변종 몬스터와 변종 영혼석이 기본 키 하나로 모이고 `variants`에 속성이 한 번만 들어간다. 보스는 `boss:<kind>`. Task 1 테스트.
- **도감을 연 채 시간이 흐르지 않음**: 원정 중 도감 팝업을 열고 닫아도 `s.time`이 그대로다. Task 4 테스트.

---

## File Structure

| 파일 | 책임 |
| --- | --- |
| `expedition/progression/codex.gd` (새) | 파일, 기록, 항목, 완성률 |
| `expedition/run/session.gd` | `codex`, `records_codex`, `codex_dirty`, 처치 기록 호출 |
| `expedition/level/continuous_floor.gd` | `observe` 끝에서 새로 본 적 기록 |
| `expedition/items/gear.gd` | `roll_part` 획득 기록 |
| `expedition/progression/essences.gd` | `absorb` 성공 기록 |
| `expedition/run/descent.gd`, `expedition/run/camp.gd` | `flush` |
| `expedition/ui/screens/start_screen.gd` | `records_codex = true`, "도감" 버튼 |
| `expedition/ui/screens/result_card.gd` | `note_run_end` + `flush` |
| `expedition/ui/main.gd` | `_notification`에서 `flush`, `show_codex(tab, focus)` |
| `expedition/ui/screens/codex_screen.gd` (새) | 도감 팝업 |
| `expedition/ui/screens/camp_screen.gd`, `floor_hud.gd`(전술 메뉴), `popups.gd`(적 정보 창) | 진입점 |
| `tests/codex.gd`, `tests/codex_ui.gd` (새) | 아래 |
| `.github/workflows/deploy-pages.yml` | 새 스위트 둘 |

---

### Task 1: `codex.gd` — 파일과 기록

**Files:**
- Create: `expedition/progression/codex.gd`
- Test: `tests/codex.gd`

**Interfaces:**
- Consumes: `Essences.canonical(id) -> String`, `Essences.variant_element(id) -> String`, `Essences.part_of(id) -> String`, `Essences.base_of(id) -> String`
- Produces:
  - `static var path := "user://codex.json"`
  - `static func empty() -> Dictionary`
  - `static func read() -> Dictionary`, `static func write(data: Dictionary) -> bool`
  - `static func monster_key(enemy: Dictionary) -> String`, `static func stone_key(id: String) -> String`
  - `static func note_seen(s, enemy: Dictionary) -> void`, `note_kill(s, enemy)`, `note_stone(s, id: String)`, `note_absorb(s, id: String)`, `note_unrand(s, id: String)`, `note_run_end(s)`
  - `static func flush(s) -> void`
  - 세션에 필요한 필드(Task 2가 선언): `s.codex: Dictionary`, `s.records_codex: bool`, `s.codex_dirty: bool`

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/codex.gd`

```gdscript
extends SceneTree
## The codex (2026-09-26 codex spec §1–2): one file across runs, written only
## by real runs, broken files set aside, unknown keys kept.
const Codex = preload("res://expedition/progression/codex.gd")
var failures := 0
var checks := 0

class FakeSession:
	var codex: Dictionary = {}
	var records_codex := true
	var codex_dirty := false
	var depth := 1

func check(ok: bool, reason: String) -> void:
	checks += 1
	if not ok: failures += 1; push_error(reason)

func _initialize() -> void: call_deferred("run")

func run() -> void:
	Codex.path = "user://test_codex_codex.json"
	wipe()
	fresh(); round_trip(); broken(); unknown_keys(); keys(); records(); off()
	wipe()
	print("Codex: %d checks, %d failures" % [checks,failures]); quit(1 if failures else 0)

func wipe() -> void:
	for p in [Codex.path,Codex.bad_path()]:
		if FileAccess.file_exists(p): DirAccess.remove_absolute(ProjectSettings.globalize_path(p))

func session() -> FakeSession:
	var s := FakeSession.new(); s.codex = Codex.read(); return s

func fresh() -> void:
	wipe()
	var data := Codex.read()
	check(data == Codex.empty(),"no file reads as an empty codex")
	check(not FileAccess.file_exists(Codex.path),"reading does not create the file")

func round_trip() -> void:
	wipe()
	var s := session()
	Codex.note_kill(s,{"species_id":"dcss_rat","enemy":true})
	check(s.codex_dirty,"a note marks the codex dirty")
	Codex.flush(s)
	check(FileAccess.file_exists(Codex.path) and not s.codex_dirty,"flush writes and clears dirty")
	check(int(Codex.read().monsters.dcss_rat.kills) == 1,"the kill survives the round trip")

func broken() -> void:
	wipe()
	var f := FileAccess.open(Codex.path,FileAccess.WRITE); f.store_string("{ not json"); f.close()
	var data := Codex.read()
	check(data == Codex.empty(),"a broken file reads as empty")
	check(FileAccess.file_exists(Codex.bad_path()) and not FileAccess.file_exists(Codex.path),"the broken file is set aside")

func unknown_keys() -> void:
	wipe()
	var start := Codex.empty(); start["future"] = {"x":1}
	Codex.write(start)
	var s := session()
	Codex.note_kill(s,{"species_id":"dcss_rat","enemy":true}); Codex.flush(s)
	check(Codex.read().get("future",{}) == {"x":1},"an unknown key is kept")

func keys() -> void:
	check(Codex.monster_key({"species_id":"dcss_rat"}) == "dcss_rat","a monster is its species")
	check(Codex.monster_key({"species_id":"goblin","boss":true,"boss_kind":"chief"}) == "boss:chief","a boss is its kind")
	check(Codex.stone_key("SPIDER_WEB/pierced@poison") == "SPIDER_WEB/pierced","a variant stone keys without its element")
	check(Codex.stone_key("RAT_GNAW") == "RAT_GNAW/cut","a bare id keys as its headline part")
	check(Codex.stone_key("no_such") == "","an unknown stone has no key")

func records() -> void:
	wipe()
	var s := session()
	var rat := {"species_id":"dcss_rat","enemy":true,"variant_element":"poison"}
	Codex.note_seen(s,rat); s.codex_dirty = false
	Codex.note_seen(s,rat)
	check(not s.codex_dirty,"seeing the same thing again changes nothing")
	check(s.codex.monsters.dcss_rat.seen and s.codex.monsters.dcss_rat.variants == ["poison"],"seen with its variant once")
	Codex.note_stone(s,"SPIDER_WEB/pierced@poison"); Codex.note_stone(s,"SPIDER_WEB/pierced")
	check(int(s.codex.stones["SPIDER_WEB/pierced"].found) == 2 and s.codex.stones["SPIDER_WEB/pierced"].variants == ["poison"],"stones pile under one key")
	Codex.note_absorb(s,"SPIDER_WEB/pierced")
	check(bool(s.codex.stones["SPIDER_WEB/pierced"].absorbed),"absorbed is kept")
	Codex.note_unrand(s,"blood_axe")
	check(int(s.codex.unrands.blood_axe.found) == 1,"an unrand is counted")
	s.depth = 7; Codex.note_run_end(s)
	check(int(s.codex.runs) == 1 and int(s.codex.deepest) == 7,"a run end counts the run and the depth")

func off() -> void:
	wipe()
	var s := session(); s.records_codex = false
	Codex.note_kill(s,{"species_id":"dcss_rat","enemy":true}); Codex.note_stone(s,"RAT_GNAW")
	Codex.flush(s)
	check(s.codex == Codex.empty() and not FileAccess.file_exists(Codex.path),"a session that does not record writes nothing")
```

- [ ] **Step 2: 실패 확인**

Run: `godot --headless --path . --script res://tests/codex.gd`
Expected: `codex.gd`가 없어 파싱 오류로 실패.

- [ ] **Step 3: `expedition/progression/codex.gd` 작성**

```gdscript
extends RefCounted
## The codex (2026-09-26 codex spec): what the player has met and gathered,
## kept across runs in one file. Only a real run records; every note marks the
## session dirty and `flush` writes it at the few moments the spec names.
const Essences = preload("res://expedition/progression/essences.gd")
const VERSION := 1
const SECTIONS := ["monsters","stones","unrands","tips"]
static var path := "user://codex.json"

static func empty() -> Dictionary:
	return {"version":VERSION,"runs":0,"deepest":0,"monsters":{},"stones":{},"unrands":{},"tips":{}}

static func bad_path() -> String:
	return path.get_basename()+".bad.json"

## An empty codex for no file; a broken file is moved aside, never overwritten.
static func read() -> Dictionary:
	if not FileAccess.file_exists(path): return empty()
	var parsed: Variant = JSON.parse_string(FileAccess.get_file_as_string(path))
	if not parsed is Dictionary:
		if FileAccess.file_exists(bad_path()): DirAccess.remove_absolute(ProjectSettings.globalize_path(bad_path()))
		DirAccess.rename_absolute(ProjectSettings.globalize_path(path),ProjectSettings.globalize_path(bad_path()))
		return empty()
	var data := empty()
	for key in parsed: data[key] = parsed[key]
	for key in SECTIONS:
		if not data[key] is Dictionary: data[key] = {}
	return data

static func write(data: Dictionary) -> bool:
	var file := FileAccess.open(path,FileAccess.WRITE)
	if file == null: return false
	file.store_string(JSON.stringify(data,"\t"))
	file.close()
	return true

static func monster_key(enemy: Dictionary) -> String:
	if bool(enemy.get("boss",false)): return "boss:"+str(enemy.get("boss_kind",""))
	return str(enemy.get("species_id",""))

## A stone's codex key: its canonical id without the variant element.
static func stone_key(id: String) -> String:
	var canonical: String = Essences.canonical(id)
	return "" if canonical.is_empty() else canonical.get_slice("@",0)

static func recording(s) -> bool:
	return s != null and bool(s.records_codex)

static func entry(s, section: String, key: String, fresh: Dictionary) -> Dictionary:
	var book: Dictionary = s.codex.get_or_add(section,{})
	if not book.has(key): book[key] = fresh; s.codex_dirty = true
	return book[key]

static func add_variant(s, row: Dictionary, element: String) -> void:
	if element.is_empty(): return
	var list: Array = row.get_or_add("variants",[])
	if element in list: return
	list.append(element); s.codex_dirty = true

static func note_seen(s, enemy: Dictionary) -> void:
	var key := monster_key(enemy)
	if not recording(s) or key.is_empty() or key == "boss:": return
	var row := entry(s,"monsters",key,{"seen":true,"kills":0,"variants":[]})
	if not bool(row.get("seen",false)): row.seen = true; s.codex_dirty = true
	add_variant(s,row,str(enemy.get("variant_element","")))

static func note_kill(s, enemy: Dictionary) -> void:
	var key := monster_key(enemy)
	if not recording(s) or key.is_empty() or key == "boss:": return
	var row := entry(s,"monsters",key,{"seen":true,"kills":0,"variants":[]})
	row.seen = true
	row.kills = int(row.get("kills",0))+1; s.codex_dirty = true
	add_variant(s,row,str(enemy.get("variant_element","")))

static func note_stone(s, id: String) -> void:
	var key := stone_key(id)
	if not recording(s) or key.is_empty(): return
	var row := entry(s,"stones",key,{"found":0,"absorbed":false,"variants":[]})
	row.found = int(row.get("found",0))+1; s.codex_dirty = true
	add_variant(s,row,Essences.variant_element(Essences.canonical(id)))

static func note_absorb(s, id: String) -> void:
	var key := stone_key(id)
	if not recording(s) or key.is_empty(): return
	var row := entry(s,"stones",key,{"found":0,"absorbed":false,"variants":[]})
	if not bool(row.get("absorbed",false)): row.absorbed = true; s.codex_dirty = true

static func note_unrand(s, id: String) -> void:
	if not recording(s) or id.is_empty(): return
	var row := entry(s,"unrands",id,{"found":0})
	row.found = int(row.get("found",0))+1; s.codex_dirty = true

static func note_run_end(s) -> void:
	if not recording(s): return
	s.codex.runs = int(s.codex.get("runs",0))+1
	s.codex.deepest = maxi(int(s.codex.get("deepest",0)),int(s.depth))
	s.codex_dirty = true

static func flush(s) -> void:
	if not recording(s) or not bool(s.codex_dirty): return
	if write(s.codex): s.codex_dirty = false
```

- [ ] **Step 4: 통과 확인**

Run: `godot --headless --path . --script res://tests/codex.gd`
Expected: `Codex: N checks, 0 failures`.

- [ ] **Step 5: Commit**

```bash
git add expedition/progression/codex.gd tests/codex.gd
git commit -m "Add the codex: one file across runs, notes for monsters, stones and unrands"
```

---

### Task 2: 원정에서 기록하기

**Files:**
- Modify: `expedition/run/session.gd` (필드, 처치 분기)
- Modify: `expedition/level/continuous_floor.gd` (`observe` 끝)
- Modify: `expedition/items/gear.gd` (`roll_part`, 보스 `grant_part` 경로)
- Modify: `expedition/progression/essences.gd` (`absorb`)
- Modify: `expedition/run/descent.gd`, `expedition/run/camp.gd` (`flush`)
- Modify: `expedition/ui/screens/start_screen.gd` (`new_run`)
- Test: `tests/codex.gd` (함수 추가)

**Interfaces:**
- Consumes: Task 1 전부
- Produces: `Session.codex`, `Session.records_codex`, `Session.codex_dirty`

- [ ] **Step 1: 실패하는 테스트 추가** — `tests/codex.gd`

맨 위 상수에:

```gdscript
const Session = preload("res://expedition/run/session.gd")
const Fixture = preload("res://tests/floor_fixture.gd")
const Gear = preload("res://expedition/items/gear.gd")
const Essences = preload("res://expedition/progression/essences.gd")
```

`run()`의 `off()` 뒤에 `real_run(); not_real()`을 넣고, 파일 끝에:

```gdscript
func real_session():
	var s = Session.new_run(731)
	s.records_codex = true; s.codex = Codex.read()
	return s

func real_run() -> void:
	wipe()
	var s = real_session()
	var c: Vector2i = Fixture.arena(s,8)
	var foe: Dictionary = s.enemies[0]
	# No stone on the kill itself: the drop below is rolled by hand.
	foe.hp = 5; foe.max_hp = 5; foe.pos = c+Vector2i(1,0); foe.species_id = "dcss_rat"; foe.variant_element = ""; foe.part_id = ""
	s.floor_state.observe(s)
	check(bool(s.codex.monsters.get("dcss_rat",{}).get("seen",false)),"a monster in sight is seen")
	s.codex_dirty = false
	s.floor_state.observe(s)
	check(not s.codex_dirty,"looking again at the same monster changes nothing")
	s.Reactions.begin_action(s)
	s.damage(foe,50,int(s.party[0].id),"physical")
	check(int(s.codex.monsters.dcss_rat.kills) == 1,"a party kill is counted")
	s.parts_bag.clear()
	foe.part_id = "RAT_GNAW"; foe.erase("part_rolled"); s.essence_seen.clear()
	Gear.roll_part(s,foe)
	var got: Array = s.parts_bag.keys()
	check(got.size() == 1 and int(s.codex.stones.get(Codex.stone_key(str(got[0])),{}).get("found",0)) == 1,"a dropped stone is found")
	s.phase = "CAMP"
	check(Essences.absorb(s,s.party[0],str(got[0])) == "","the stone is absorbed")
	check(bool(s.codex.stones[Codex.stone_key(str(got[0]))].absorbed),"an absorbed stone is marked")

func not_real() -> void:
	wipe()
	var s = Session.new_run(731)
	check(not s.records_codex,"a session does not record unless told")
	var c: Vector2i = Fixture.arena(s,8)
	var foe: Dictionary = s.enemies[0]
	foe.hp = 5; foe.pos = c+Vector2i(1,0); foe.species_id = "dcss_rat"
	s.floor_state.observe(s)
	s.damage(foe,50,int(s.party[0].id),"physical")
	Gear.grant_test_loadout(s)
	Codex.flush(s)
	check(not FileAccess.file_exists(Codex.path),"a test session leaves no codex file")
```

- [ ] **Step 2: 실패 확인**

Run: `godot --headless --path . --script res://tests/codex.gd`
Expected: `records_codex`가 세션에 없어 오류.

- [ ] **Step 3: `session.gd` — 필드와 처치 기록**

상수 목록에 `const Codex = preload("res://expedition/progression/codex.gd")`.

`var essence_seen: Dictionary = {}` 근처에:

```gdscript
## The codex read at the start of a run, and whether this run writes to it:
## only a run begun from the start screen does (codex spec §0.4).
var codex: Dictionary = {}
var records_codex := false
var codex_dirty := false
```

처치 분기에서 `if party_hunted:` 블록(현재 `battle_stats.kills`를 더하는 곳) 안에 한 줄:

```gdscript
		if party_hunted:
			battle_stats.kills = int(battle_stats.get("kills",0))+1
			run_stats.kills = int(run_stats.kills)+1
			score += 10
			Codex.note_kill(self,target)
```

- [ ] **Step 4: `continuous_floor.gd` — 새로 본 적**

`observe(s)` 함수의 **맨 끝**(시야 계산이 끝난 뒤)에:

```gdscript
	# The codex hears of every monster that has come into sight (codex spec §1).
	if s.records_codex:
		for enemy in s.enemies:
			if int(enemy.hp) > 0 and visible.has(enemy.pos): s.Codex.note_seen(s,enemy)
```

(`Codex.note_seen`은 이미 본 것이면 아무것도 바꾸지 않는다.)

- [ ] **Step 5: `gear.gd` — 획득 기록**

`roll_part`의 `s.parts_bag[id] = int(s.parts_bag.get(id,0))+1` 바로 아래에:

```gdscript
	s.Codex.note_stone(s,id)
```

보스 영혼석을 주는 경로(`session.gd`의 `grant_part(str(target.part_id))`가 부르는 함수)에서도 가방에 넣는 줄 바로 아래 같은 한 줄. `grant_test_loadout`에는 넣지 않는다.

- [ ] **Step 6: `essences.gd` — 흡수 기록**

`absorb`에서 `known[id] = 1` 바로 아래에:

```gdscript
	s.Codex.note_absorb(s,id)
```

(`essences.gd`는 `Codex`를 preload하지 않는다. `codex.gd`가 `essences.gd`를 preload하므로 순환을 피해 세션 상수로 부른다.)

- [ ] **Step 7: 쓰기 시점**

- `expedition/run/descent.gd`의 `descend`에서 `s.depth += 1` 줄 바로 앞에 `s.Codex.flush(s)`.
- `expedition/run/camp.gd`에서 `s.phase = "CAMP"` 줄 바로 뒤에 `s.Codex.flush(s)`.

- [ ] **Step 8: `start_screen.gd` — 실제 원정만 기록**

`new_run(ui)`의 `ui.session = Session.new_run(randi(),ui.kit_choice)` 바로 아래에:

```gdscript
	ui.session.records_codex = true
	ui.session.codex = Session.Codex.read()
```

- [ ] **Step 9: 통과 확인**

Run: `godot --headless --path . --script res://tests/codex.gd`
Expected: `0 failures`.

- [ ] **Step 10: 회귀**

Run: `for t in continuous_floor floor_descent camping essences part_stones parts difficulty_gate arena_mode; do echo "== $t"; godot --headless --path . --script res://tests/$t.gd 2>&1 | grep -E "failures|ERROR" | head -3; done; ls "$HOME/.local/share/godot/app_userdata/"*/codex.json 2>/dev/null`
Expected: 모두 `0 failures`. 마지막 `ls`는 아무것도 찍지 않는다(테스트가 실제 도감 파일을 만들지 않음). 프로젝트의 `user://` 실제 위치가 다르면 `godot --headless --path . -e --quit` 없이 `ProjectSettings.globalize_path("user://")`를 찍는 한 줄 스크립트로 찾아 확인한다.

- [ ] **Step 11: Commit**

```bash
git add expedition/run/session.gd expedition/level/continuous_floor.gd expedition/items/gear.gd expedition/progression/essences.gd expedition/run/descent.gd expedition/run/camp.gd expedition/ui/screens/start_screen.gd tests/codex.gd
git commit -m "Record sightings, kills, drops and absorptions in the codex during real runs"
```

---

### Task 3: 화면용 항목과 완성률

**Files:**
- Modify: `expedition/progression/codex.gd`
- Test: `tests/codex.gd`

**Interfaces:**
- Consumes: `Essences.row(id)`(`part_name`, `effect`, `species`), `Essences.catalog()`(정규 부위 id 목록), `Encounters.table()`(`data/content/floor_monsters.json` 종족 행: `species_id`, `display_name`, `zone`, `role`), `Forms.body_line(actor)`, `Forms.FORMS`, `Forms.PARTS`, `BossAI.KINDS`, `data/content/stone_effects.json`(`effects[id].text`, `keywords`, `families`)
- Produces:
  - `static func monster_list() -> Array` — `[{key, zone, boss}]` 구역 순, 보스는 끝
  - `static func monster_entry(data, key) -> Dictionary` — `{key, known, name, zone, body_line, effect_text, parts:[{id, name, form, found}], kills, variants}`. 모르면 `known=false`와 `zone`만
  - `static func stone_list() -> Array` — 부위 영혼석 키 목록(종족 순, 부위 순 cut·broken·pierced) + 보스 영혼석
  - `static func stone_entry(data, key) -> Dictionary` — `{key, found, absorbed, species_known, name, text, keywords, families, hint}`
  - `static func completion(data) -> Dictionary` — `{monsters:[n,total], stones:[n,total], unrands:[n,total]}`

- [ ] **Step 1: 실패하는 테스트 추가** — `run()`에 `entries()` 추가, 파일 끝에:

```gdscript
func entries() -> void:
	var data := Codex.empty()
	var rat := Codex.monster_entry(data,"dcss_rat")
	check(not rat.known and int(rat.zone) == 1 and not rat.has("name"),"an unseen monster shows only its zone")
	data.monsters["dcss_rat"] = {"seen":true,"kills":3,"variants":["poison"]}
	rat = Codex.monster_entry(data,"dcss_rat")
	check(rat.known and rat.name == "쥐" and int(rat.kills) == 3,"a seen monster shows its name and kills")
	check(rat.parts.size() == 3 and rat.parts.map(func(p): return p.form) == ["SLASH","IMPACT","PIERCE"],"its three parts in form order")
	check(rat.body_line.begins_with("공격 "),"its body line")
	var locked := Codex.stone_entry(data,"RAT_GNAW/broken")
	check(not locked.found and locked.species_known and locked.hint == "타격으로 마무리","a locked part of a seen species hints its form")
	var hidden := Codex.stone_entry(Codex.empty(),"RAT_GNAW/broken")
	check(not hidden.species_known and hidden.name == "???","a part of an unseen species hides its name")
	data.stones["RAT_GNAW/cut"] = {"found":1,"absorbed":false,"variants":[]}
	var got := Codex.stone_entry(data,"RAT_GNAW/cut")
	check(got.found and not got.text.is_empty() and not got.families.is_empty(),"a found part shows its effect and families")
	var done := Codex.completion(data)
	check(int(done.monsters[0]) == 1 and int(done.monsters[1]) == Codex.monster_list().size(),"monster completion")
	check(int(done.stones[0]) == 1 and int(done.stones[1]) == Codex.stone_list().size(),"stone completion")
	check(Codex.monster_list().filter(func(m): return m.boss).size() == 4,"four bosses on the monster list")
```

- [ ] **Step 2: 실패 확인** → `monster_entry` 없음.

- [ ] **Step 3: 구현** — `codex.gd`에 추가

```gdscript
const Forms = preload("res://expedition/combat/forms.gd")
const Encounters = preload("res://expedition/level/encounter_builder.gd")
const BossAI = preload("res://expedition/actors/boss_ai.gd")
const ZONE_OF_BOSS := {"chief":1,"golem":2,"eater":3,"fallen":4}
const FORM_HINT := {"SLASH":"베기로 마무리","IMPACT":"타격으로 마무리","PIERCE":"찌르기로 마무리"}
static var effects: Dictionary = JSON.parse_string(FileAccess.get_file_as_string("res://data/content/stone_effects.json")).get("effects",{})

static func monster_list() -> Array:
	var rows: Array = Encounters.table().duplicate()
	rows.sort_custom(func(a,b): return int(a.get("zone",9)) < int(b.get("zone",9)) if int(a.get("zone",9)) != int(b.get("zone",9)) else str(a.species_id) < str(b.species_id))
	var list: Array = rows.map(func(r): return {"key":str(r.species_id),"zone":int(r.get("zone",0)),"boss":false})
	for kind in BossAI.KINDS: list.append({"key":"boss:"+str(kind),"zone":int(ZONE_OF_BOSS.get(kind,0)),"boss":true})
	return list

static func species_stone(species_id: String) -> String:
	for id in Essences.content.rows:
		if str(Essences.content.rows[id].get("species","")) == species_id: return str(id)
	return ""

static func monster_entry(data: Dictionary, key: String) -> Dictionary:
	var row: Dictionary = data.get("monsters",{}).get(key,{})
	var zone := 0
	for m in monster_list():
		if m.key == key: zone = int(m.zone)
	if not bool(row.get("seen",false)): return {"key":key,"known":false,"zone":zone}
	var result := {"key":key,"known":true,"zone":zone,"kills":int(row.get("kills",0)),"variants":row.get("variants",[]),"parts":[]}
	if key.begins_with("boss:"):
		result.name = key.trim_prefix("boss:"); result.body_line = ""; result.effect_text = ""
		return result
	var species: Dictionary = Encounters.species(key)
	result.name = str(species.get("display_name",key))
	result.body_line = Forms.body_line({"enemy":true,"species_id":key})
	var base := species_stone(key)
	result.effect_text = str(effects.get(Essences.row(base).get("effect",""),{}).get("text","")) if not base.is_empty() else ""
	for i in range(Forms.PARTS.size()):
		var id: String = "%s/%s" % [base,Forms.PARTS[i]]
		if base.is_empty() or not Essences.has(id): continue
		result.parts.append({"id":id,"name":str(Essences.row(id).get("part_name","")),"form":Forms.FORMS[i],
			"found":int(data.get("stones",{}).get(id,{}).get("found",0)) > 0})
	return result

static func stone_list() -> Array:
	var list: Array = []
	for m in monster_list():
		if m.boss: continue
		var base := species_stone(str(m.key))
		for part in Forms.PARTS:
			var id: String = "%s/%s" % [base,part]
			if not base.is_empty() and Essences.has(id): list.append(id)
	for id in Essences.content.rows:
		if not Essences.content.rows[id].has("parts") and Essences.has(str(id)): list.append(str(id))
	return list

static func stone_entry(data: Dictionary, key: String) -> Dictionary:
	var row: Dictionary = data.get("stones",{}).get(key,{})
	var info: Dictionary = Essences.row(key)
	var species: String = str(info.get("species",""))
	var known: bool = bool(data.get("monsters",{}).get(species,{}).get("seen",false))
	var part := Essences.part_of(key)
	var form: String = Forms.FORMS[Forms.PARTS.find(part)] if part in Forms.PARTS else ""
	var effect: Dictionary = effects.get(str(info.get("effect","")),{})
	return {"key":key,"found":int(row.get("found",0)) > 0,"absorbed":bool(row.get("absorbed",false)),"species_known":known,
		"name":str(info.get("part_name",Essences.title(key))) if known or int(row.get("found",0)) > 0 else "???",
		"text":str(effect.get("text","")),"keywords":effect.get("keywords",[]),"families":effect.get("families",[]),
		"hint":str(FORM_HINT.get(form,""))}

static func completion(data: Dictionary) -> Dictionary:
	var monsters := monster_list(); var stones := stone_list()
	var unrands: Array = unrand_ids()
	return {"monsters":[monsters.filter(func(m): return bool(data.get("monsters",{}).get(m.key,{}).get("seen",false))).size(),monsters.size()],
		"stones":[stones.filter(func(id): return int(data.get("stones",{}).get(id,{}).get("found",0)) > 0).size(),stones.size()],
		"unrands":[unrands.filter(func(id): return int(data.get("unrands",{}).get(id,{}).get("found",0)) > 0).size(),unrands.size()]}

## The fixed artefacts' ids, empty until `data/content/unrands.json` exists.
static func unrand_ids() -> Array:
	if not FileAccess.file_exists("res://data/content/unrands.json"): return []
	var parsed: Variant = JSON.parse_string(FileAccess.get_file_as_string("res://data/content/unrands.json"))
	if not parsed is Dictionary: return []
	return (parsed.get("unrands",{}) as Dictionary).keys()
```

`Essences.row(id)`가 `part_name`·`effect`·`species`를 이 이름으로 돌려주지 않으면, 실제 키 이름을 `essences.gd`의 `row`에서 확인해 위 세 곳을 맞춘다(④-a1 결과에 따름). `Encounters.species(id)`가 없으면 `encounter_builder.gd`의 같은 역할 함수를 쓴다.

- [ ] **Step 4: 통과 확인** → `0 failures`.

- [ ] **Step 5: Commit**

```bash
git add expedition/progression/codex.gd tests/codex.gd
git commit -m "Give the codex its monster and stone entries and completion counts"
```

---

### Task 4: 도감 화면과 진입점

**Files:**
- Create: `expedition/ui/screens/codex_screen.gd`
- Modify: `expedition/ui/main.gd` (`show_codex`, `_notification`)
- Modify: `expedition/ui/screens/start_screen.gd`, `camp_screen.gd`, `floor_hud.gd`(전술 메뉴 `show_manual_tactics`), `popups.gd`(`show_enemy_info`), `result_card.gd`
- Modify: `expedition/art/mobile_art.gd` (실루엣은 `paint`에 검은 `modulate`만 쓰므로 새 함수 불필요 — 화면이 `TextureRect.modulate = Color.BLACK`)
- Test: `tests/codex_ui.gd`
- Modify: `.github/workflows/deploy-pages.yml`

**Interfaces:**
- Consumes: Task 3 항목 함수, `Art.enemy_sprite(species_id) -> AtlasTexture`, `ui.label(parent,text,size)`, `ui.button(parent,text,callback,enabled)`, `ui.clear(node)`, `ui.modal_content`, `ui.details_popup`, `ui.popup_width()`
- Produces: `main.gd`의 `func show_codex(tab: String = "monsters", focus: String = "") -> void`; 노드 이름 `CodexScreen`, `CodexTab_monsters`/`_stones`/`_unrands`, `CodexEntry_<key>`, `CodexDetail`, `CodexFilter_<빌드군 번호>`, `CodexCompletion`

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/codex_ui.gd`

```gdscript
extends SceneTree
## The codex screen (codex spec §3): three tabs, unknown monsters as
## silhouettes, locked parts with their form hint, a build family filter, and
## opening it mid-run lets no time pass.
const Session = preload("res://expedition/run/session.gd")
const Codex = preload("res://expedition/progression/codex.gd")
var failures := 0
var checks := 0

func check(ok: bool, reason: String) -> void:
	checks += 1
	if not ok: failures += 1; push_error(reason)

func _initialize() -> void: call_deferred("run")

func node(scene, name: String) -> Node:
	return scene.modal_content.find_child(name,true,false)

func run() -> void:
	Codex.path = "user://test_codex_ui.json"
	var scene = load("res://expedition/ui/main.tscn").instantiate()
	var s = Session.new_run(731)
	s.codex = Codex.empty()
	s.codex.monsters["dcss_rat"] = {"seen":true,"kills":2,"variants":[]}
	s.codex.stones["RAT_GNAW/cut"] = {"found":1,"absorbed":true,"variants":[]}
	scene.session = s; root.size = Vector2i(390,844); root.add_child(scene); scene.set_process(false)
	for _i in range(4): await process_frame
	var before: int = int(s.time)
	scene.show_codex()
	for _i in range(3): await process_frame
	check(scene.details_popup.visible and node(scene,"CodexScreen") != null,"the codex opens")
	check(node(scene,"CodexCompletion") != null,"completion is shown")
	check(node(scene,"CodexEntry_dcss_rat") != null and node(scene,"CodexEntry_cave_spider") != null,"seen and unseen monsters are listed")
	check(node(scene,"CodexEntry_cave_spider").get_meta("known",true) == false,"an unseen monster is a silhouette")
	scene.show_codex("monsters","dcss_rat")
	for _i in range(3): await process_frame
	check(node(scene,"CodexDetail") != null and str(node(scene,"CodexDetail").get_meta("key","")) == "dcss_rat","focus opens the rat")
	scene.show_codex("stones")
	for _i in range(3): await process_frame
	check(node(scene,"CodexEntry_RAT_GNAW/cut") != null and node(scene,"CodexEntry_RAT_GNAW/broken") != null,"found and locked parts are listed")
	check(node(scene,"CodexFilter_1") != null,"a build family filter exists")
	(node(scene,"CodexFilter_12") as Button).pressed.emit()
	for _i in range(3): await process_frame
	check(node(scene,"CodexEntry_RAT_GNAW/cut") != null,"the support filter keeps the rat's tail")
	check(node(scene,"CodexTab_unrands") == null or FileAccess.file_exists("res://data/content/unrands.json"),"the gear tab hides until unrands exist")
	check(int(s.time) == before,"no time passes in the codex")
	scene.details_popup.hide()
	print("Codex UI: %d checks, %d failures" % [checks,failures]); quit(1 if failures else 0)
```

(`CodexEntry_RAT_GNAW/cut`처럼 `/`가 들어간 노드 이름을 Godot가 허용하지 않으면, 화면과 테스트 모두 `/`를 `_`로 바꾼 이름을 쓴다: `CodexEntry_RAT_GNAW_cut`. 한 곳에서 `name_for(key)` 함수로 만든다.)

- [ ] **Step 2: 실패 확인** → `show_codex` 없음.

- [ ] **Step 3: `codex_screen.gd` 작성**

```gdscript
extends RefCounted
## The codex popup (codex spec §3): monsters, soul stones, fixed artefacts.
const Codex = preload("res://expedition/progression/codex.gd")
const Art = preload("res://expedition/art/mobile_art.gd")
const FAMILY_NAMES := {1:"출혈",2:"분쇄",3:"급소",4:"광폭",5:"수호",6:"사수",7:"원소",8:"저주",9:"독",10:"소환",11:"사령",12:"지원"}

static func name_for(key: String) -> String:
	return "CodexEntry_"+key.replace("/","_").replace(":","_")

static func data_of(ui) -> Dictionary:
	return ui.session.codex if ui.session != null and not ui.session.codex.is_empty() else Codex.read()

static func show(ui, tab: String = "monsters", focus: String = "", family: int = 0) -> void:
	var data := data_of(ui)
	ui.clear(ui.modal_content)
	ui.modal_content.custom_minimum_size = Vector2(ui.popup_width(),0)
	var box := VBoxContainer.new(); box.name = "CodexScreen"; ui.modal_content.add_child(box)
	var header := HBoxContainer.new(); box.add_child(header)
	var title = ui.label(header,"도감",18); title.size_flags_horizontal = Control.SIZE_EXPAND_FILL
	ui.button(header,"×",func(): ui.details_popup.hide()).custom_minimum_size.x = 44
	var done := Codex.completion(data)
	var line = ui.label(box,"몬스터 %d/%d · 영혼석 %d/%d" % [done.monsters[0],done.monsters[1],done.stones[0],done.stones[1]],12)
	line.name = "CodexCompletion"
	var tabs := HBoxContainer.new(); box.add_child(tabs)
	var names := {"monsters":"몬스터","stones":"영혼석"}
	if not Codex.unrand_ids().is_empty(): names["unrands"] = "장비"
	for id in names:
		var b = ui.button(tabs,str(names[id]),func(): show(ui,id)); b.name = "CodexTab_"+str(id)
		b.toggle_mode = true; b.button_pressed = id == tab
	var scroll := ScrollContainer.new(); scroll.custom_minimum_size = Vector2(ui.popup_width(),520); box.add_child(scroll)
	var list := VBoxContainer.new(); list.size_flags_horizontal = Control.SIZE_EXPAND_FILL; scroll.add_child(list)
	match tab:
		"monsters": monsters(ui,list,data,focus)
		"stones": stones(ui,list,data,family)
		"unrands": unrands(ui,list,data)
	ui.details_popup.popup_centered()

static func monsters(ui, list: VBoxContainer, data: Dictionary, focus: String) -> void:
	if not focus.is_empty():
		detail(ui,list,Codex.monster_entry(data,focus)); return
	var zone := -1; var grid: GridContainer = null
	for m in Codex.monster_list():
		if int(m.zone) != zone or (m.boss and grid != null and grid.name != "Bosses"):
			zone = int(m.zone)
			ui.label(list,"보스" if m.boss else "구역 %d" % zone,14)
			grid = GridContainer.new(); grid.columns = 4; grid.name = "Bosses" if m.boss else "Zone%d" % zone; list.add_child(grid)
		var entry := Codex.monster_entry(data,str(m.key))
		var cell := VBoxContainer.new(); cell.name = name_for(str(m.key)); cell.set_meta("known",entry.known); grid.add_child(cell)
		var picture := TextureRect.new(); picture.custom_minimum_size = Vector2(64,64)
		picture.expand_mode = TextureRect.EXPAND_IGNORE_SIZE; picture.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
		if not m.boss: picture.texture = Art.enemy_sprite(str(m.key))
		if not entry.known: picture.modulate = Color.BLACK
		cell.add_child(picture)
		ui.button(cell,str(entry.get("name","???")),func(): show(ui,"monsters",str(m.key)),entry.known)

static func detail(ui, list: VBoxContainer, entry: Dictionary) -> void:
	var box := VBoxContainer.new(); box.name = "CodexDetail"; box.set_meta("key",entry.key); list.add_child(box)
	ui.button(box,"← 목록",func(): show(ui,"monsters"))
	ui.label(box,"%s · 구역 %d" % [str(entry.get("name","???")),int(entry.zone)],18)
	if not entry.known: return
	if not str(entry.body_line).is_empty(): ui.label(box,str(entry.body_line),13)
	if not str(entry.effect_text).is_empty(): ui.label(box,"대표 효과 · "+str(entry.effect_text),13).autowrap_mode = TextServer.AUTOWRAP_WORD_SMART
	for part in entry.parts:
		ui.label(box,"%s — %s%s" % [str(part.name),str(Codex.FORM_HINT.get(part.form,"")),"  ✓" if part.found else ""],13)
	ui.label(box,"처치 %d" % int(entry.kills),12)

static func stones(ui, list: VBoxContainer, data: Dictionary, family: int) -> void:
	var chips := HFlowContainer.new(); list.add_child(chips)
	for f in FAMILY_NAMES:
		var chip = ui.button(chips,str(FAMILY_NAMES[f]),func(): show(ui,"stones","",0 if family == f else f)); chip.name = "CodexFilter_%d" % f
		chip.toggle_mode = true; chip.button_pressed = family == f
	for id in Codex.stone_list():
		var entry := Codex.stone_entry(data,str(id))
		if family > 0 and family not in entry.families: continue
		var row := VBoxContainer.new(); row.name = name_for(str(id)); list.add_child(row)
		var head = ui.label(row,str(entry.name)+("  ✓흡수" if entry.absorbed else ""),14)
		if not entry.found:
			head.modulate = Color(1,1,1,0.45)
			if entry.species_known: ui.label(row,str(entry.hint),12)
			continue
		var text = ui.label(row,str(entry.text),12); text.autowrap_mode = TextServer.AUTOWRAP_WORD_SMART
		ui.label(row," · ".join(entry.families.map(func(f): return str(FAMILY_NAMES.get(int(f),"")))),11)

static func unrands(ui, list: VBoxContainer, data: Dictionary) -> void:
	for id in Codex.unrand_ids():
		var found: bool = int(data.get("unrands",{}).get(id,{}).get("found",0)) > 0
		var row = ui.label(list,str(id) if found else "???",14); row.name = name_for(str(id))
```

(`Codex.FORM_HINT`는 Task 3에서 `const`로 만들었다. 고정 아티팩트 이름·효과 표시는 장비 설계 구현 때 `unrands.json` 행을 읽도록 넓힌다.)

- [ ] **Step 4: `main.gd`**

`preload` 목록에 `const CodexScreen = preload("res://expedition/ui/screens/codex_screen.gd")`, 그리고:

```gdscript
func show_codex(tab: String = "monsters", focus: String = "") -> void: CodexScreen.show(self,tab,focus)

## The codex is written when the app is put away or closed (codex spec §2.2).
func _notification(what: int) -> void:
	if what in [NOTIFICATION_APPLICATION_PAUSED,NOTIFICATION_WM_CLOSE_REQUEST] and session != null:
		session.Codex.flush(session)
```

`main.gd`에 `_notification`이 이미 있으면 그 안에 위 두 줄을 더한다.

- [ ] **Step 5: 진입점**

- `start_screen.gd`: `var arena = ui.button(actions,"전투 시험",...)` 줄 아래에

  ```gdscript
  	var codex = ui.button(actions,"도감",func(): ui.show_codex()); codex.name = "CodexButton"; codex.custom_minimum_size.y = 56
  ```
- `camp_screen.gd` `build_camp_screen`: 야영 메뉴 버튼 줄(장비·영혼석 버튼이 있는 곳)에 `ui.button(<그 줄>,"도감",func(): ui.show_codex()).name = "CampCodex"`.
- `floor_hud.gd` `show_manual_tactics`: `var skills = ui.button(box,"기술",...)` 줄 아래에 `ui.button(box,"도감",func(): ui.show_codex()).name = "TacticCodex"`.
- `popups.gd` `show_enemy_info`: `title` 라벨을 만든 줄 아래에 `ui.button(ui.modal_content,"도감",func(): ui.show_codex("monsters",Session.Codex.monster_key(enemy))).name = "EnemyCodex"` (적 정보 창을 닫고 도감을 여는 것은 `show`가 모달 내용을 바꾸므로 따로 닫지 않는다).
- `result_card.gd` `build_result_card`: 첫 줄(`var session = ui.session`) 아래에

  ```gdscript
  	if not bool(session.get_meta("codex_run_ended",false)):
  		session.set_meta("codex_run_ended",true)
  		session.Codex.note_run_end(session); session.Codex.flush(session)
  ```

  (결과 카드는 화면 갱신마다 다시 그려질 수 있어 한 번만 센다.)

- [ ] **Step 6: CI** — `.github/workflows/deploy-pages.yml` 스위트 목록의 `essences` 뒤에 `codex codex_ui`.

- [ ] **Step 7: 확인**

Run: `for t in codex codex_ui inspect_ui ui_smoke camping character_ui mobile_hud; do echo "== $t"; godot --headless --path . --script res://tests/$t.gd 2>&1 | grep -E "failures|ERROR" | head -3; done`
Expected: 모두 `0 failures`. 진입점 버튼 추가로 버튼 수·배치를 정확히 세던 UI 검사(`mobile_hud` 등)가 깨지면, 검사 의도를 유지하며 새 버튼을 반영한다.

- [ ] **Step 8: 전체 CI 스위트**

```bash
fail=0; for suite in $(sed -n 's/.*for suite in \(.*\); do/\1/p' .github/workflows/deploy-pages.yml); do out=$(godot --headless --path . --script "res://tests/${suite}.gd" 2>&1); if echo "$out" | grep -q "SCRIPT ERROR\|ERROR"; then echo "FAIL $suite"; fail=1; fi; done; echo "fail=$fail"
```

Expected: `fail=0`.

- [ ] **Step 9: Commit**

```bash
git add expedition/ui/screens/codex_screen.gd expedition/ui/main.gd expedition/ui/screens/start_screen.gd expedition/ui/screens/camp_screen.gd expedition/ui/screens/floor_hud.gd expedition/ui/screens/popups.gd expedition/ui/screens/result_card.gd tests/codex_ui.gd .github/workflows/deploy-pages.yml
git commit -m "Open the codex from the start screen, camp, tactics and the enemy card"
```

---

## 끝난 뒤 확인

- 새 탐험 → 몬스터 한 마리 보고 잡기 → 야영 → 앱 종료 → 다시 켜서 도감에 쥐가 "처치 1"로 있음(`run` 스킬이나 수동으로).
- 전투 시험(아레나)을 한 판 해도 도감 파일이 바뀌지 않음.
- 스펙 대응: §1 기록 → Task 2, §2 파일 → Task 1, §3 화면·진입점·완성률 → Task 3·4, §5 테스트 → 각 Task.
