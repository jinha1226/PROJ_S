# 구역과 층 구현 계획 (1/4)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 한 판을 12층 4구역으로 나누고, 구역마다 테마·전용 방·보스 방 배치·고유 위험 요소·층 주 속성 목록을 두며, 깊은 층 보정을 없애고 12층 보스를 잡으면 승리하게 한다.

**Architecture:** 새 정적 모듈 `expedition/level/zones.gd`가 층 → 구역 → 테마·주 속성·보스 방을 정한다. 새 정적 모듈 `expedition/level/hazards.gd`가 위험 요소의 초기값·배치·환경 틱·시야·색을 맡는다. 생성기와 층 적용은 이 둘을 읽기만 한다. 보스의 행동은 이 계획의 범위가 아니다. 지금의 보스 셋(수렁 포식자, 폭탄 암살자, 과부하 거인)은 새 보스 방에서 그대로 싸우고, 계획 4/4가 교체한다.

**Tech Stack:** Godot 4.6 GDScript, 헤드리스 테스트(`godot --headless --path . --script res://tests/<suite>.gd`), JSON 콘텐츠.

**Spec:** `docs/superpowers/specs/2026-09-25-zones-bosses-reactions-design.md` (§0.1, §1, §3.2의 깊은 층 보정 삭제, §8의 테마·템플릿·변종·결과 행)

**연계 계획:** 2/4 `2026-09-25-bestiary.md`(종족 30종, `zone`·`family`·`role`·`variants` 필드), 3/4 `2026-09-25-reactions.md`(빌드 축과 원소 반응, 칸 필드 `steam_until`·`ice`·`poison_pool`), 4/4 `2026-09-25-bosses.md`(보스 넷, `Zones.boss_template`과 보스 방 id 사용, 최종 보스 처치에서 `Session.victory()` 호출).

## Global Constraints

- 테스트 실행 전 임포트: `godot --headless --path . --editor --import --quit`. 새 `.gd` 파일을 만든 뒤에도 한 번 더 돌린다.
- 모든 스위트는 `push_error`가 없고 종료 코드 0이어야 통과. 로그에 `SCRIPT ERROR:`나 `^ERROR:` 줄이 있으면 실패.
- 결정성: 게임 쪽 무작위는 `Hexaco.sample(seed, key, lane, modulus)`, 층 생성 쪽은 생성기의 `RandomNumberGenerator`(시드 고정)만 쓴다. `randi()` 금지.
- 메시지·라벨은 한국어.
- 공유 이름(네 계획 공통, 바꾸지 않는다):
  - `Zones.FINAL_DEPTH := 12`, `Zones.FLOORS_PER_ZONE := 3`, `Zones.zone_of(depth) -> int`(1~4, 12 초과는 4), `Zones.theme_id(depth) -> String`(`F1_RUINS`, `F2_MINES`, `F3_TEMPLE`, `F4_CRYPT`), `Zones.is_boss_floor(depth) -> bool`(`depth % 3 == 0`), `Zones.elements(zone) -> Array`, `Zones.floor_element(s, depth) -> String`(없으면 `""`), `Zones.boss_template(zone) -> String`(`throne_room`, `foundry`, `binding_altar`, `crypt_heart`).
  - 위험 요소 칸: 지형 `lava`(불 늘 100, 줄지 않음), 지형 `deep_water`(이동 ×1.5, 늘 전도, 서 있으면 젖음), 지형 `bog`(늘 젖음 100, 서 있으면 중독), 칸 bool `gas`(불이 닿으면 반경 2 폭발 피해 16, 주변 불 +40), 칸 사전 `collapse` `{"armed":bool,"at":int}`, 칸 bool `fog`(안에서 시야 반경 −3). 계획 3/4가 `steam_until`, `ice`, `poison_pool`을 더한다.
  - 세션: 단계 `"VICTORY"`, `Session.victory() -> void`(여러 번 불러도 한 번만 적용).
- **종족의 구역 필드 대체 규칙**: 계획 2/4가 종족 행에 `zone`을 넣기 전까지, `zone`이 없는 종족은 기존 깊이 곡선(`min_depth`~`max_depth`)을 따르되 6층보다 깊게 읽지 않는다. 즉 7~12층은 6층 표로 뽑는다. 이 규칙은 `encounter_builder.gd` `weight`의 주석과 이 계획의 Task 3에 적어 둔다. `zone`이 있는 종족은 자기 구역의 세 층에서만, 희귀도 그대로 나온다.
- 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`. 커밋은 `git commit -m ... -- <이 작업의 파일들>`처럼 경로를 명시한다. 다른 세션의 미커밋 변경을 건드리지 않는다.
- 검사 수를 줄이지 않는다. 옛 검사를 지우면 같은 수 이상의 새 검사로 바꾼다.

---

## File Structure

| 파일 | 역할 |
|---|---|
| `expedition/level/zones.gd` (신규) | 층 → 구역, 테마 id, 보스 층, 구역 주 속성 목록, 층 주 속성, 보스 방 id, 구역 이름 |
| `expedition/level/hazards.gd` (신규) | 위험 요소 초기값, 생성기 배치, 환경 틱 규칙(용암·깊은 물·늪·가스·무너지는 천장), 안개 시야, 그림 색 |
| `data/content/floor_templates.json` | 구역 전용 방 8개, 보스 방 4개, `boss_lair` 삭제, 깊이 범위 1~12 |
| `expedition/level/floor_templates.gd` | 새 글리프 `L D B G F`(용암, 깊은 물, 늪, 가스, 안개), 파싱 결과의 `hazards` |
| `data/content/floor_themes.json` | `F3_TEMPLE`, `F4_CRYPT` 추가, 구역별 싸움 방 목록·장식·위험 요소 개수 |
| `data/content/combat.json` | `move_cost`에 `lava`, `deep_water`, `bog` |
| `expedition/level/floor_generator.gd` | 보스 방 id 일반화(`relic_template`), 위험 요소 배치, 위험 지형 위에 조사물·아이템 금지 |
| `expedition/level/continuous_floor.gd` | 구역으로 테마 고르기, 깊은 층 보정 삭제, 칸 초기값과 위험 요소 복사, 안개 시야 |
| `expedition/level/encounter_builder.gd` | 종족 `zone` 필드로 뽑기, 없으면 깊이 대체 규칙, 한 단계 깊은 뽑기는 구역 안에서만 |
| `expedition/level/variants.gd` | 층 주 속성을 `Zones.floor_element`로 |
| `expedition/actors/boss_ai.gd` | 보스 방 찾기를 네 보스 방으로, 깊은 층 보정 삭제(나머지는 계획 4/4) |
| `expedition/actors/npc_roster.gd` | NPC가 위험 지형 위에 서지 않음 |
| `expedition/time/scheduler.gd` | 환경 틱에서 위험 요소 규칙, `VICTORY`에서 시간 멈춤 |
| `expedition/run/session.gd` | `conductive`에 깊은 물, `victory()`, 12층 보스 처치 시 승리 |
| `expedition/run/descent.gd` | 12층에서 더 내려가지 않음 |
| `expedition/ui/board.gd`, `expedition/art/floor1_art.gd`, `expedition/art/mobile_art.gd` | 새 테마와 위험 요소 그리기 |
| `expedition/ui/main.gd`, `expedition/ui/screens/result_card.gd` | 승리 결과 카드 |
| `tests/zones.gd`, `tests/hazards.gd` (신규) | 새 규칙 검사 |

---

### Task 1: 구역 모듈

**Files:**
- Create: `expedition/level/zones.gd`
- Test: `tests/zones.gd` (신규)

**Interfaces:**
- Consumes: `Hexaco.sample(seed, key, lane, modulus)` (`res://sim/dungeon_population/hexaco_profile.gd`)
- Produces: Global Constraints의 `Zones` 이름 전부, 그리고 `Zones.first_floor(zone) -> int`, `Zones.last_floor(zone) -> int`, `Zones.zone_name(zone) -> String`, `Zones.THEMES`, `Zones.BOSS_TEMPLATES`, `Zones.ELEMENTS`

- [ ] **Step 1: 실패하는 테스트 쓰기**

`tests/zones.gd`:

```gdscript
extends SceneTree
## Four zones of three floors: which theme a floor wears, which element it
## leans to, where its boss lives, and the twelfth floor that ends the run.
const Session = preload("res://expedition/run/session.gd")
const Zones = preload("res://expedition/level/zones.gd")
var failures := 0
var checks := 0

func check(ok: bool, reason: String) -> void:
	checks += 1
	if not ok: failures += 1; push_error(reason)

func _initialize() -> void: call_deferred("run")

func run() -> void:
	table()
	elements()
	print("Zones: %d checks, %d failures" % [checks,failures]); quit(1 if failures else 0)

func table() -> void:
	var zones: Array = range(1,13).map(func(d): return Zones.zone_of(d))
	check(zones == [1,1,1,2,2,2,3,3,3,4,4,4],"three floors a zone (%s)" % [zones])
	check(Zones.zone_of(0) == 1 and Zones.zone_of(15) == 4,"out-of-range floors clamp to the first and last zone")
	check(Zones.theme_id(1) == "F1_RUINS" and Zones.theme_id(5) == "F2_MINES" and Zones.theme_id(8) == "F3_TEMPLE" and Zones.theme_id(12) == "F4_CRYPT","each zone wears its own theme")
	check(range(1,13).filter(func(d): return Zones.is_boss_floor(d)) == [3,6,9,12],"a boss on every third floor")
	check(Zones.FINAL_DEPTH == 12 and Zones.FLOORS_PER_ZONE == 3,"twelve floors, three a zone")
	check(Zones.first_floor(3) == 7 and Zones.last_floor(3) == 9,"zone three spans floors seven to nine")
	check([1,2,3,4].map(func(z): return Zones.boss_template(z)) == ["throne_room","foundry","binding_altar","crypt_heart"],"each zone has its boss room")
	check(Zones.zone_name(2) == "불타는 폐광" and Zones.zone_name(4) == "망자의 묘역","zones are named")

func elements() -> void:
	check(Zones.elements(1) == ["poison"] and Zones.elements(2) == ["fire","air"] and Zones.elements(3) == ["ice","poison"] and Zones.elements(4) == ["will"],"each zone lists its elements")
	var s = Session.new(731)
	check(Zones.floor_element(s,1) == "" and Zones.floor_element(s,2) == "","the first two floors lean to nothing")
	check(Zones.floor_element(s,3) == "poison","the third floor leans to poison")
	var seen: Dictionary = {}
	for seed_value in range(40):
		var t = Session.new(seed_value)
		for depth in range(3,13):
			var element: String = Zones.floor_element(t,depth)
			check(element in Zones.elements(Zones.zone_of(depth)),"floor %d leans to its zone's element (seed %d)" % [depth,seed_value])
			check(element == Zones.floor_element(t,depth),"the element is deterministic")
			if depth in [4,5,6]: seen[element] = true
	check(seen.has("fire") and seen.has("air"),"both of the mines' elements turn up (%s)" % [seen.keys()])
```

- [ ] **Step 2: 실패 확인**

Run: `godot --headless --path . --editor --import --quit; godot --headless --path . --script res://tests/zones.gd`
Expected: FAIL — `zones.gd`를 불러올 수 없음.

- [ ] **Step 3: `zones.gd` 쓰기**

`expedition/level/zones.gd`:

```gdscript
extends RefCounted
## The run's four zones (spec §1): three floors each, a boss on every third
## floor, a main element per floor drawn from the zone's own list. The twelfth
## floor's boss ends the run.
const Hexaco = preload("res://sim/dungeon_population/hexaco_profile.gd")
const FINAL_DEPTH := 12
const FLOORS_PER_ZONE := 3
const ZONE_COUNT := 4
const THEMES := ["F1_RUINS","F2_MINES","F3_TEMPLE","F4_CRYPT"]
const NAMES := ["무너진 성채 지하","불타는 폐광","잠긴 지하 신전","망자의 묘역"]
const BOSS_TEMPLATES := ["throne_room","foundry","binding_altar","crypt_heart"]
## Zone one leans to nothing on its first two floors and to poison on its third.
const ELEMENTS := {1:["poison"],2:["fire","air"],3:["ice","poison"],4:["will"]}
## Floors before this lean to no element at all.
const FIRST_ELEMENT_DEPTH := 3

static func zone_of(depth: int) -> int:
	return clampi((maxi(1,depth)-1)/FLOORS_PER_ZONE+1,1,ZONE_COUNT)

static func first_floor(zone: int) -> int:
	return (clampi(zone,1,ZONE_COUNT)-1)*FLOORS_PER_ZONE+1

static func last_floor(zone: int) -> int:
	return clampi(zone,1,ZONE_COUNT)*FLOORS_PER_ZONE

static func theme_id(depth: int) -> String:
	return str(THEMES[zone_of(depth)-1])

static func zone_name(zone: int) -> String:
	return str(NAMES[clampi(zone,1,ZONE_COUNT)-1])

static func is_boss_floor(depth: int) -> bool:
	return depth > 0 and depth % FLOORS_PER_ZONE == 0

static func elements(zone: int) -> Array:
	return (ELEMENTS.get(clampi(zone,1,ZONE_COUNT),[]) as Array).duplicate()

## The floor's main element, "" when it leans to none. Deterministic per run seed.
static func floor_element(s, depth: int) -> String:
	if depth < FIRST_ELEMENT_DEPTH: return ""
	var pool := elements(zone_of(depth))
	if pool.is_empty(): return ""
	return str(pool[Hexaco.sample(int(s.seed_value),depth,"zone_element",pool.size())])

static func boss_template(zone: int) -> String:
	return str(BOSS_TEMPLATES[clampi(zone,1,ZONE_COUNT)-1])
```

- [ ] **Step 4: 통과 확인**

Run: `godot --headless --path . --editor --import --quit; godot --headless --path . --script res://tests/zones.gd`
Expected: `Zones: N checks, 0 failures`.

- [ ] **Step 5: 커밋**

```bash
git add expedition/level/zones.gd tests/zones.gd
git commit -m "Add the four-zone table: themes, boss floors, floor elements

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>" -- expedition/level/zones.gd tests/zones.gd
```

---

### Task 2: 구역 전용 방과 보스 방 템플릿

**Files:**
- Modify: `expedition/level/floor_templates.gd` (`GLYPH_TERRAIN`, 새 `GLYPH_HAZARD`, `parse`)
- Modify: `data/content/floor_templates.json`
- Modify: `tests/floor_templates.gd`

**Interfaces:**
- Consumes: 없음
- Produces: 템플릿 id `guard_post`, `broken_hall`(구역 1), `water_channel`, `smelter`(구역 2), `flooded_hall`, `altar_room`(구역 3), `ossuary`, `grave_garden`(구역 4), 보스 방 `throne_room`, `foundry`, `binding_altar`, `crypt_heart`. `Templates.parse(rows)` 결과에 `hazards: {Vector2i: {"gas":true} | {"fog":true}}`. 글리프: `L`→`lava`, `D`→`deep_water`, `B`→`bog`, `G`→돌 바닥 + 가스, `F`→돌 바닥 + 안개. `boss_lair`는 이 작업에서 남겨 두고 Task 3에서 지운다.

보스 방은 배치만 정한다. 모두 몬스터 자리 `M` 하나, 계단 `>` 하나, 아래쪽 문 둘을 가진다(지금의 `boss_lair`와 같은 규칙). `binding_altar`에는 지금의 9층 보스(과부하 거인)가 쓰는 전력탑 `Y`를 계획 4/4가 교체할 때까지 둔다. `throne_room`의 물(`~`)은 지금의 3층 보스(수렁 포식자)가 회복에 쓴다.

- [ ] **Step 1: 실패하는 테스트로 바꾸기**

`tests/floor_templates.gd`의 `var ids := [...]` 줄을 다음으로 바꾼다.

```gdscript
	var ids := ["entry_camp","descent","flooded_cistern","collapsed_store","sealed_treasury","timber_gallery","boss_lair",
		"guard_post","broken_hall","water_channel","smelter","flooded_hall","altar_room","ossuary","grave_garden",
		"throne_room","foundry","binding_altar","crypt_heart"]
```

`check(Templates.definition("nope").is_empty(),"unknown template is empty")` 줄 바로 아래에 넣는다.

```gdscript
	for id in ["guard_post","broken_hall","water_channel","smelter","flooded_hall","altar_room","ossuary","grave_garden"]:
		var zoned: Dictionary = Templates.parse(Templates.definition(id).rows)
		check(zoned.anchor != Vector2i(-1,-1),"%s places its pack leader" % id)
		check("fight" in Templates.definition(id).tags,"%s is a fight room" % id)
	for id in ["throne_room","foundry","binding_altar","crypt_heart"]:
		var lair: Dictionary = Templates.parse(Templates.definition(id).rows)
		check(lair.anchor != Vector2i(-1,-1) and lair.doors.size() == 2,"%s has an anchor and two doors" % id)
		check(lair.features.values().filter(func(f): return f.kind == "stairs").size() == 1,"%s has stairs" % id)
		check("boss" in Templates.definition(id).tags and not bool(Templates.definition(id).orient),"%s is an unrotated boss room" % id)
	check(Templates.parse(Templates.definition("binding_altar").rows).features.values().any(func(f): return f.kind == "pylon"),"the altar keeps a pylon until the boss plan replaces it")
	check(Templates.parse(Templates.definition("smelter").rows).terrain.values().has("lava"),"the smelter has lava")
	check(Templates.parse(Templates.definition("smelter").rows).hazards.values().filter(func(h): return bool(h.get("gas",false))).size() == 2,"the smelter has two gas pockets")
	check(Templates.parse(Templates.definition("flooded_hall").rows).terrain.values().has("deep_water"),"the flooded hall has deep water")
	check(Templates.parse(Templates.definition("altar_room").rows).terrain.values().has("bog"),"the altar room has bog")
	check(Templates.parse(Templates.definition("grave_garden").rows).hazards.values().any(func(h): return bool(h.get("fog",false))),"the grave garden has fog")
	check(Templates.parse(Templates.definition("descent").rows).hazards.is_empty(),"an old room has no hazards")
	check(Templates.content.templates.all(func(t): return int(t.depth[1]) >= 12),"every room can appear down to floor twelve")
```

- [ ] **Step 2: 실패 확인**

Run: `godot --headless --path . --script res://tests/floor_templates.gd`
Expected: FAIL — `template guard_post exists` 등.

- [ ] **Step 3: 글리프와 파싱**

`expedition/level/floor_templates.gd`의 `GLYPH_TERRAIN`을 다음으로 바꾸고, 바로 아래에 `GLYPH_HAZARD`를 둔다.

```gdscript
const GLYPH_TERRAIN := {"#":"wall","+":"wall",".":"stone",",":"rubble","~":"water","=":"wood","%":"metal",
	"L":"lava","D":"deep_water","B":"bog","G":"stone","F":"stone",
	"@":"stone",">":"stone","Y":"stone","&":"stone","!":"stone","$":"stone","^":"stone","A":"stone","C":"stone","M":"stone","P":"stone"}
## Tile flags a glyph leaves on stone floor: a gas pocket or a bank of fog.
const GLYPH_HAZARD := {"G":{"gas":true},"F":{"fog":true}}
```

`parse`의 `var result := {...}` 줄에 `"hazards":{}`를 더한다.

```gdscript
	var result := {"width":rows[0].length(),"height":rows.size(),"terrain":{},"doors":[],"features":{},
		"anchor":Vector2i(-1,-1),"backline":[],"floor":[],"hazards":{}}
```

`parse` 루프 안의 `var feature := feature_for(glyph)` 줄 바로 위에 넣는다.

```gdscript
			if GLYPH_HAZARD.has(glyph): result.hazards[p] = (GLYPH_HAZARD[glyph] as Dictionary).duplicate()
```

- [ ] **Step 4: 템플릿 데이터**

Run:

```bash
python3 - <<'EOF'
import json
p='data/content/floor_templates.json'
d=json.load(open(p,encoding='utf-8'))
for t in d['templates']: t['depth']=[int(t['depth'][0]),12] if t['id']!='descent' else t['depth']
new=[
 {"id":"guard_post","label":"경비 초소","tags":["ruins","fight"],"depth":[1,12],"orient":True,"rows":[
  "#####+######","#..........#","#.##....##.#","#.#P....P#.#","#..........#","#....M.....#","#..........#","#.##....##.#","#..........#","######+#####"]},
 {"id":"broken_hall","label":"무너진 회랑","tags":["ruins","fight"],"depth":[1,12],"orient":True,"rows":[
  "##############","#..,,....,,..#","#.#,.#..#.,#.#","+..,..M...,..+","#.#,.#..#.,#.#","#..,,..P.,,..#","#......P.....#","##############"]},
 {"id":"water_channel","label":"수로 갱도","tags":["mines","fight"],"depth":[1,12],"orient":True,"rows":[
  "######+######","#...~~~~~...#","#.%.~...~.%.#","#...~.M.~...#","#.P.~...~.P.#","#...~~.~~...#","#...........#","#..%.....%..#","######+######"]},
 {"id":"smelter","label":"용광로 방","tags":["mines","fight"],"depth":[1,12],"orient":True,"rows":[
  "#####+######","#..........#","#.LL....%..#","#.LL..M....#","#......G...#","#.%..P...P.#","#....G.....#","#..........#","######+#####"]},
 {"id":"flooded_hall","label":"침수 회랑","tags":["temple","fight"],"depth":[1,12],"orient":True,"rows":[
  "######+#######","#DDDD....DDDD#","#DD..P..P..DD#","#D....M.....D#","#D..........D#","#DD........DD#","#DDDD....DDDD#","#............#","#######+######"]},
 {"id":"altar_room","label":"제단 방","tags":["temple","fight"],"depth":[1,12],"orient":True,"rows":[
  "#####+######","#BB......BB#","#B..%..%..B#","#....M.....#","#..P....P..#","#....%%....#","#..........#","#B........B#","#BB......BB#","######+#####"]},
 {"id":"ossuary","label":"납골당","tags":["crypt","fight"],"depth":[1,12],"orient":True,"rows":[
  "######+######","#...........#","#.#.#...#.#.#","#.....M.....#","#.#.#...#.#.#","#..P.....P..#","#.#.#...#.#.#","#...........#","######+######"]},
 {"id":"grave_garden","label":"무덤 정원","tags":["crypt","fight"],"depth":[1,12],"orient":True,"rows":[
  "#####+#######","#FF.......FF#","#F.,.,.,.,.F#","#...........#","#..,..M..,..#","#.P.......P.#","#..,.,.,.,..#","#F.........F#","#FF.......FF#","#######+#####"]},
 {"id":"throne_room","label":"족장의 왕좌","tags":["boss"],"depth":[3,12],"orient":False,"rows":[
  "##############","#............#","#.,...==...,.#","#.....=M=....#","#............#","#..~~....~~..#","#............#","#.,........,.#","#......>.....#","#............#","######+##+####"]},
 {"id":"foundry","label":"수로 제련소","tags":["boss"],"depth":[3,12],"orient":False,"rows":[
  "##############","#~~~......~~~#","#~..........~#","#~..LL..LL..~#","#.....M......#","#..LL....LL..#","#............#","#~..........~#","#~~~..>...~~~#","#............#","######+##+####"]},
 {"id":"binding_altar","label":"구속의 제단","tags":["boss"],"depth":[3,12],"orient":False,"rows":[
  "##############","#............#","#.%...BB...%.#","#.....M......#","#....%%%.....#","#..Y.........#","#............#","#.%.........%#","#......>.....#","#............#","######+##+####"]},
 {"id":"crypt_heart","label":"묘역의 심장","tags":["boss"],"depth":[3,12],"orient":False,"rows":[
  "##############","#............#","#.#..#..#..#.#","#............#","#.....M......#","#..,......,..#","#............#","#.#..#..#..#.#","#......>.....#","#............#","######+##+####"]},
]
d['templates'].extend(new)
for t in d['templates']:
    w=len(t['rows'][0]); assert all(len(r)==w for r in t['rows']),t['id']
json.dump(d,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=2)
open(p,'a',encoding='utf-8').write('\n')
EOF
git diff --stat data/content/floor_templates.json
```

Expected: 오류 없이 끝나고 `floor_templates.json`에 추가만 있다. 스크립트의 `assert`는 행 길이가 모두 같은지 확인한다. 기존 파일의 들여쓰기가 2칸이 아니면 `indent` 값을 기존 파일에 맞춘다(`head -5 data/content/floor_templates.json`으로 확인).

- [ ] **Step 5: 통과 확인**

Run: `godot --headless --path . --script res://tests/floor_templates.gd; for t in floor_generator floor_descent boss_floor; do godot --headless --path . --script "res://tests/$t.gd" 2>&1 | tail -1; done`
Expected: `floor_templates` 0 failures. 나머지 셋도 0 failures(새 템플릿은 아직 어떤 테마도 쓰지 않는다).

- [ ] **Step 6: 커밋**

```bash
git commit -m "Add zone fight rooms, boss rooms and hazard glyphs

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>" -- expedition/level/floor_templates.gd data/content/floor_templates.json tests/floor_templates.gd
```

---

### Task 3: 구역으로 테마 고르기, 보스 방 일반화, 깊은 층 보정 삭제

**Files:**
- Modify: `data/content/floor_themes.json` (`F3_TEMPLE`, `F4_CRYPT` 추가, 싸움 방 목록·장식)
- Modify: `data/content/floor_templates.json` (`boss_lair` 삭제)
- Modify: `expedition/level/continuous_floor.gd` (`theme_for`, `strongest_pack` 호출, 깊은 층 보정 삭제, `mint_enemy`)
- Modify: `expedition/level/floor_generator.gd` (`relic_template`, `random_origin`, `is_fight_room`, `choose_encounter_rooms`, `build_encounters`, `validate`)
- Modify: `expedition/level/encounter_builder.gd` (`weight`, `attempt`)
- Modify: `expedition/actors/boss_ai.gd` (보스 방 찾기 두 곳, 깊은 층 보정)
- Modify (기존 테스트): `tests/floor_descent.gd`, `tests/boss_floor.gd`, `tests/floor_templates.gd`, `tests/caster_species.gd`, `tests/integration.gd`, `tests/parts.gd`, `tests/playthrough.gd`, `tests/solo_floor.gd`
- Test: `tests/zones.gd` (함수 추가)

**Interfaces:**
- Consumes: Task 1의 `Zones`, Task 2의 템플릿 id
- Produces: `theme_for(depth)` 결과에 `zone: int`, 보스 층이면 `boss_template: String`. `theme.deep` 키와 `Floor.deep_scale`, `Floor.last_catalog_depth`, `DEEP_HP_STEP`, `DEEP_ATTACK_STEP`는 사라진다. 모든 몬스터의 `attack_percent`는 100이다(계획 2/4가 구역 기준값으로 수치를 정한다). `Generator.relic_template(theme) -> String`. `Encounters.FALLBACK_DEPTH := 6`.

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/zones.gd`의 머리에 `const Floor = preload("res://expedition/level/continuous_floor.gd")`, `const Generator = preload("res://expedition/level/floor_generator.gd")`, `const Encounters = preload("res://expedition/level/encounter_builder.gd")`를 더하고, `run()`의 `elements()` 다음에 `themes()`를 넣은 뒤 파일 끝에 붙인다.

```gdscript
func themes() -> void:
	for depth in range(1,13):
		var theme: Dictionary = Floor.theme_for(depth)
		check(theme.id == Zones.theme_id(depth) and int(theme.zone) == Zones.zone_of(depth),"floor %d wears its zone's theme" % depth)
		check(not theme.has("deep"),"floor %d carries no deep scaling" % depth)
		check(bool(theme.boss) == Zones.is_boss_floor(depth),"floor %d is a boss floor exactly on the third" % depth)
		if theme.boss:
			check(str(theme.boss_template) == Zones.boss_template(Zones.zone_of(depth)) and theme.boss_template in theme.templates.required,"floor %d requires its zone's boss room" % depth)
			check("descent" not in theme.templates.required,"a boss room brings its own stairs on floor %d" % depth)
		for seed_value in range(2):
			var layout: Dictionary = Generator.generate(theme,seed_value,depth)
			check(Generator.validate(layout,theme).is_empty(),"floor %d seed %d generates a valid layout" % [depth,seed_value])
	for id in ["F3_TEMPLE","F4_CRYPT"]:
		var theme: Dictionary = Generator.theme(id)
		check(theme.size == 80 and theme.rooms.count == [13,16] and theme.corridor.width == 2,"%s keeps the floor shape" % id)
	check(Floor.theme_for(1).templates.fight_pool.has("guard_post") and Floor.theme_for(4).templates.fight_pool.has("smelter"),"zones fight in their own rooms")
	check(Floor.theme_for(7).templates.fight_pool.has("flooded_hall") and Floor.theme_for(10).templates.fight_pool.has("grave_garden"),"the deeper zones too")
	var s = Session.new_run(731)
	s.depth = 11
	var member := {"species_id":"dcss_orc","display_name":"오크","max_health":70,"pos":Vector2i(1,1),"role":"MELEE"}
	var deep: Dictionary = Floor.mint_enemy(s,member,"T11","early",false)
	check(int(deep.attack_percent) == 100,"depth alone no longer raises a monster's attack")
	check(not FileAccess.get_file_as_string("res://expedition/level/continuous_floor.gd").contains("deep_scale"),"the deep scaling is gone")

func zoned_species() -> void:
	var row := {"species_id":"test","rarity":100,"zone":3,"min_depth":1,"max_depth":1,"curve":"FLAT"}
	check(Encounters.weight(row,8) == 100.0 and Encounters.weight(row,6) == 0.0 and Encounters.weight(row,10) == 0.0,"a zoned species lives only in its zone")
	var old: Dictionary = Encounters.species("dcss_orc")
	check(Encounters.weight(old,11) == Encounters.weight(old,6),"an unzoned species reads the sixth floor's table below it")
```

`run()`에 `zoned_species()`도 넣는다.

- [ ] **Step 2: 실패 확인**

Run: `godot --headless --path . --script res://tests/zones.gd`
Expected: FAIL — `floor 5 wears its zone's theme` 등.

- [ ] **Step 3: 테마 데이터**

Run:

```bash
python3 - <<'EOF'
import json,copy
p='data/content/floor_themes.json'
d=json.load(open(p,encoding='utf-8'))
t=d['themes']
t['F1_RUINS']['label']='무너진 성채 지하'
t['F1_RUINS']['templates']['fight_pool']=["flooded_cistern","collapsed_store","timber_gallery","guard_post","broken_hall"]
t['F2_MINES']['label']='불타는 폐광'
t['F2_MINES']['depth']=4
t['F2_MINES']['templates']['fight_pool']=["collapsed_store","timber_gallery","water_channel","smelter"]
temple=copy.deepcopy(t['F2_MINES'])
temple.update({"label":"잠긴 지하 신전","depth":7})
temple['palette']={"floor":"stone","accents":["water","metal"],"accent_ratio":0.15}
temple['templates']['fight_pool']=["flooded_cistern","collapsed_store","flooded_hall","altar_room"]
crypt=copy.deepcopy(t['F2_MINES'])
crypt.update({"label":"망자의 묘역","depth":10})
crypt['palette']={"floor":"stone","accents":["rubble","wood"],"accent_ratio":0.12}
crypt['templates']['fight_pool']=["collapsed_store","timber_gallery","ossuary","grave_garden"]
t['F3_TEMPLE']=temple; t['F4_CRYPT']=crypt
json.dump(d,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=1)
open(p,'a',encoding='utf-8').write('\n')
q='data/content/floor_templates.json'
e=json.load(open(q,encoding='utf-8'))
e['templates']=[x for x in e['templates'] if x['id']!='boss_lair']
json.dump(e,open(q,'w',encoding='utf-8'),ensure_ascii=False,indent=2)
open(q,'a',encoding='utf-8').write('\n')
EOF
```

(위험 지형 장식과 위험 요소 개수는 Task 5에서 더한다.)

- [ ] **Step 4: `continuous_floor.gd`**

머리에 `const Zones = preload("res://expedition/level/zones.gd")`를 넣고, `const DEEP_HP_STEP := 12`, `const DEEP_ATTACK_STEP := 8` 두 줄을 지운다.

`theme_for`, `last_catalog_depth`, `deep_scale` 세 함수를 다음 하나로 바꾼다.

```gdscript
## The floor's theme: its zone's own, with the pack budget growing from the
## third floor down as before, capped at what the catalog can fill here. A boss
## floor swaps the stairs room for its zone's boss room, which has stairs.
static func theme_for(depth: int) -> Dictionary:
	var zone: int = Zones.zone_of(depth)
	var theme: Dictionary = Generator.theme(Zones.theme_id(depth))
	if depth >= 3:
		var scale: float = 1.0+0.25*(depth-2)
		theme.monsters.max_members = mini(4,2+int(depth/3))
		var cap: int = strongest_pack(depth,int(theme.monsters.max_members))*4/5
		for key in theme.monsters.budget: theme.monsters.budget[key] = mini(roundi(theme.monsters.budget[key]*scale),cap)
	theme.depth = depth
	theme.zone = zone
	theme.boss = Zones.is_boss_floor(depth)
	if theme.boss:
		theme.boss_template = Zones.boss_template(zone)
		theme.templates.required = ["entry_camp",theme.boss_template,"sealed_treasury"]
	return theme
```

`mint_enemy`의 세 줄

```gdscript
	var deep: Dictionary = deep_scale(int(s.depth))
	enemy.hp = enemy.hp*int(deep.hp)/100
	enemy.max_hp = enemy.hp
	enemy.attack_percent = int(deep.attack)
```

를 다음으로 바꾼다.

```gdscript
	enemy.max_hp = enemy.hp
	enemy.attack_percent = 100
```

- [ ] **Step 5: 생성기의 보스 방 일반화**

`expedition/level/floor_generator.gd` 머리에 `const Zones = preload("res://expedition/level/zones.gd")`를 넣고, `static func outer(...)` 바로 위에 둔다.

```gdscript
## The room the floor runs toward: its boss room on a boss floor, else the stairs.
static func relic_template(theme: Dictionary) -> String:
	if theme.get("boss",false): return str(theme.get("boss_template",Zones.boss_template(Zones.zone_of(int(theme.get("depth",3))))))
	return "descent"
```

다섯 곳을 바꾼다.
- `random_origin`: `elif template_id in ["descent","boss_lair"]: min_x = maxi(min_x,size*2/3)` → `elif template_id == "descent" or template_id in Zones.BOSS_TEMPLATES: min_x = maxi(min_x,size*2/3)`
- `is_fight_room`: `room.template_id in ["descent","boss_lair"]` → `(room.template_id == "descent" or room.template_id in Zones.BOSS_TEMPLATES)`
- `choose_encounter_rooms`: `var relic := room_index(rooms,"boss_lair" if theme.get("boss",false) else "descent")` → `var relic := room_index(rooms,relic_template(theme))`
- `build_encounters`: `if theme.get("boss",false) and room.template_id == "boss_lair": continue` → `if theme.get("boss",false) and room.template_id == relic_template(theme): continue`. 같은 함수의 `Encounters.fill(rng,mini(depth,6),budget,...)`에서 `mini(depth,6)`을 `depth`로 바꾼다(대체 규칙은 `weight`가 맡는다).
- `validate`: `room_index(rooms,"boss_lair" if theme.get("boss",false) else "descent")` → `room_index(rooms,relic_template(theme))`

- [ ] **Step 6: 종족 뽑기**

`expedition/level/encounter_builder.gd` 머리에 넣는다.

```gdscript
const Zones = preload("res://expedition/level/zones.gd")
## A species with no "zone" field (the catalog before the bestiary plan) is
## drawn by its old depth curve, read no deeper than this floor's table.
const FALLBACK_DEPTH := 6
```

`weight`를 바꾼다.

```gdscript
## A zoned species lives in its zone's three floors at its full rarity. An
## unzoned one follows its depth curve, floors below FALLBACK_DEPTH reading the
## FALLBACK_DEPTH table: the bestiary plan (2/4) gives every species a zone.
static func weight(row: Dictionary, depth: int) -> float:
	if row.has("zone"): return float(row.rarity) if int(row.zone) == Zones.zone_of(depth) else 0.0
	return float(row.rarity)*curve(row,mini(depth,FALLBACK_DEPTH))
```

`attempt`의 `if ood and rng.randi_range(1,100) <= OOD_PERCENT: table_depth = depth+1`을 다음으로 바꾼다.

```gdscript
	# A pack from one floor deeper, but never from the next zone's list.
	if ood and rng.randi_range(1,100) <= OOD_PERCENT: table_depth = mini(depth+1,Zones.last_floor(Zones.zone_of(depth)))
```

- [ ] **Step 7: 보스 방 찾기와 보스 체력**

`expedition/actors/boss_ai.gd` 머리에 `const Zones = preload("res://expedition/level/zones.gd")`를 넣는다.
- `spawn`의 `if room.template_id == "boss_lair": lair = room; break` → `if room.template_id in Zones.BOSS_TEMPLATES: lair = room; break`
- `spawn`의 세 줄

```gdscript
	var deep: Dictionary = s.Floor.deep_scale(depth)
	boss.hp = (64+8*(int(depth/3)-1))*int(deep.hp)/100; boss.max_hp = boss.hp
	boss.attack_percent = int(deep.attack)
```

을 다음으로 바꾼다(계획 4/4가 보스 체력을 구역 기준값으로 다시 정한다).

```gdscript
	boss.hp = 64+8*(int(depth/3)-1); boss.max_hp = boss.hp
	boss.attack_percent = 100
```

- `turn`의 `if room.template_id != "boss_lair": continue` → `if room.template_id not in Zones.BOSS_TEMPLATES: continue`

- [ ] **Step 8: 기존 테스트 고치기**

- `tests/floor_descent.gd`
  - `var cap: int = Floor.strongest_pack(6,int(deep.monsters.max_members))*4/5` → `var cap: int = Floor.strongest_pack(depth,int(deep.monsters.max_members))*4/5`
  - `check(Floor.theme_for(1).id == "F1_RUINS" and Floor.theme_for(2).id == "F2_MINES" and Floor.theme_for(3).id == "F1_RUINS","alternating themes")` → `check(Floor.theme_for(1).id == "F1_RUINS" and Floor.theme_for(3).id == "F1_RUINS" and Floor.theme_for(4).id == "F2_MINES" and Floor.theme_for(10).id == "F4_CRYPT","themes follow the zones")`
  - `for id in ["F1_RUINS","F2_MINES"]:` → `for id in ["F1_RUINS","F2_MINES","F3_TEMPLE","F4_CRYPT"]:`
  - `check(s.descend() and s.depth == 2 and s.floor_state.layout.theme_id == "F2_MINES","descent builds next theme")` → `check(s.descend() and s.depth == 2 and s.floor_state.layout.theme_id == "F1_RUINS","the second floor stays in the first zone")`
- `tests/boss_floor.gd`
  - 머리에 `const Zones = preload("res://expedition/level/zones.gd")`
  - `for depth in [3,6,9]:` → `for depth in [3,6,9,12]:`
  - `check(theme.boss and "boss_lair" in theme.templates.required,"boss theme %d" % depth)` → `check(theme.boss and Zones.boss_template(Zones.zone_of(depth)) in theme.templates.required,"boss theme %d" % depth)`
  - `check(boss.hp == (64+8*(depth/3-1))*int(s.Floor.deep_scale(depth).hp)/100,"boss health scales with depth")` → `check(boss.hp == 64+8*(depth/3-1),"boss health grows by the boss floor, not by deep scaling")`
- `tests/floor_templates.gd`
  - Task 2에서 넣은 ids 목록에서 `"boss_lair",`를 지운다.
  - `var lair: Dictionary = Templates.parse(Templates.definition("boss_lair").rows)`부터 `boss lair has stairs` 검사까지 네 줄을 다음으로 바꾼다(검사 3 → 3).

```gdscript
	check(Templates.definition("boss_lair").is_empty(),"the old boss lair is gone")
	check(["throne_room","foundry","binding_altar","crypt_heart"].all(func(id): return Templates.parse(Templates.definition(id).rows).doors.size() == 2),"every boss room has two doors")
	check(["throne_room","foundry","binding_altar","crypt_heart"].all(func(id): return Templates.parse(Templates.definition(id).rows).anchor != Vector2i(-1,-1)),"every boss room seats its boss")
```

- `tests/caster_species.gd`의 `func deep() -> void:` 본문 전체를 다음으로 바꾼다(검사 7 → 7).

```gdscript
func deep() -> void:
	check(not Floor.theme_for(10).has("deep"),"the theme carries no deep scaling")
	check(not FileAccess.get_file_as_string("res://expedition/level/continuous_floor.gd").contains("DEEP_HP_STEP"),"no deep health step")
	check(not FileAccess.get_file_as_string("res://expedition/level/continuous_floor.gd").contains("DEEP_ATTACK_STEP"),"no deep attack step")
	check(int(Floor.theme_for(12).depth) == 12,"the twelfth floor still builds its theme")
	var s = Session.new_run(731)
	var member := {"species_id":"goblin","display_name":"고블린","max_health":28,"pos":Vector2i(1,1),"role":"MELEE"}
	s.depth = 8
	var shallow: Dictionary = Floor.mint_enemy(s,member,"T8","early",false)
	s.depth = 11
	var deeper: Dictionary = Floor.mint_enemy(s,member,"T11","early",false)
	check(int(shallow.attack_percent) == 100 and int(deeper.attack_percent) == 100,"depth alone no longer raises attack")
	check(int(deeper.max_hp) == int(shallow.max_hp),"depth alone no longer raises health")
	check(int(deeper.hp) == int(deeper.max_hp),"a fresh monster is at full health")
```

- 두 번째 층이 이제 구역 1(`F1_RUINS`)이다. 다음 네 줄의 `"F2_MINES"`를 `"F1_RUINS"`로 바꾸고 메시지를 고친다.
  - `tests/integration.gd`: `run_session.floor_state.layout.theme_id == "F2_MINES","food persists and theme changes"` → `run_session.floor_state.layout.theme_id == "F1_RUINS","food persists and the first zone continues"`
  - `tests/parts.gd`: `s.floor_state.layout.theme_id == "F2_MINES","descent makes next floor"` → `s.floor_state.layout.theme_id == "F1_RUINS","descent makes the next floor of the zone"`
  - `tests/playthrough.gd`: `s.floor_state.layout.theme_id == "F2_MINES" and s.floor_state.layout.stairs.x >= 0,"next floor is a new mine layout"` → `s.floor_state.layout.theme_id == "F1_RUINS" and s.floor_state.layout.stairs.x >= 0,"next floor is a new layout of the first zone"`
  - `tests/solo_floor.gd` 두 곳: `"second floor changes theme"`와 `"the second floor is the mines"` 검사의 `"F2_MINES"` → `"F1_RUINS"`, 메시지는 각각 `"the second floor stays in the first zone"`, `"the second floor is still the ruins"`.

- [ ] **Step 9: 통과 확인**

Run: `godot --headless --path . --editor --import --quit; for t in zones floor_templates floor_descent boss_floor floor_generator continuous_floor caster_species encounter_builder integration parts playthrough solo_floor variants; do godot --headless --path . --script "res://tests/$t.gd" 2>&1 | tail -1; done`
Expected: 모두 0 failures.

- [ ] **Step 10: 커밋**

```bash
git commit -m "Pick themes and boss rooms by zone; drop deep-floor scaling

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>" -- data/content/floor_themes.json data/content/floor_templates.json expedition/level/continuous_floor.gd expedition/level/floor_generator.gd expedition/level/encounter_builder.gd expedition/actors/boss_ai.gd tests/zones.gd tests/floor_descent.gd tests/boss_floor.gd tests/floor_templates.gd tests/caster_species.gd tests/integration.gd tests/parts.gd tests/playthrough.gd tests/solo_floor.gd
```

---

### Task 4: 층 주 속성을 구역 목록에서

**Files:**
- Modify: `expedition/level/variants.gd` (`floor_element` 삭제, `assign`)
- Test: `tests/zones.gd` (함수 추가)

**Interfaces:**
- Consumes: `Zones.floor_element(s, depth)`
- Produces: `floor_state.element`는 `Zones.floor_element(s, s.depth)`와 같다. 변종 규칙(주 속성 변종이 절반 이상)은 그대로다.

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/zones.gd`의 `run()`에 `floors()`를 넣고 파일 끝에 붙인다.

```gdscript
func floors() -> void:
	for seed_value in range(1,6):
		for depth in [1,2,3,5,8,11]:
			var s = Session.new_run(seed_value)
			s.depth = depth; s.floor_state.build(s)
			var element: String = str(s.floor_state.element)
			check(element == Zones.floor_element(s,depth),"floor %d leans to the zone's pick (seed %d)" % [depth,seed_value])
			var variants: Array = s.enemies.filter(func(e): return not str(e.get("variant_element","")).is_empty())
			if depth <= 2: check(variants.is_empty(),"floor %d has base species only (seed %d)" % [depth,seed_value])
			var outside: Array = variants.filter(func(e): return e.variant_element not in Zones.elements(Zones.zone_of(depth)))
			check(outside.size()*2 <= variants.size(),"most variants on floor %d wear the zone's elements (seed %d)" % [depth,seed_value])
```

- [ ] **Step 2: 실패 확인**

Run: `godot --headless --path . --script res://tests/zones.gd`
Expected: FAIL — `floor 2 has base species only` 또는 `floor N leans to the zone's pick`.

- [ ] **Step 3: `variants.gd`**

머리에 `const Zones = preload("res://expedition/level/zones.gd")`를 넣는다. `floor_element` 함수(주석 포함)를 지우고, `assign`의 첫 줄 `var element := floor_element(int(s.seed_value),int(s.depth),enemies)`를 다음으로 바꾼다.

```gdscript
	var element := Zones.floor_element(s,int(s.depth))
```

`assign` 위 주석의 "Returns the floor's element ("" on the first floor)."를 "Returns the floor's element ("" on the first two floors)."로 바꾼다.

- [ ] **Step 4: 통과 확인**

Run: `for t in zones variants caster_species; do godot --headless --path . --script "res://tests/$t.gd" 2>&1 | tail -1; done`
Expected: 모두 0 failures. `variants.gd` 스위트의 `variants appear below the first floor` 합계 검사는 2층이 빠져도 3~7층으로 20을 넘는다. 넘지 못하면 그 테스트의 `for depth in range(2,8):`를 `range(3,10)`으로 넓힌다(검사를 지우지 않는다).

- [ ] **Step 5: 커밋**

```bash
git commit -m "Draw each floor's main element from its zone's list

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>" -- expedition/level/variants.gd tests/zones.gd tests/variants.gd
```

---

### Task 5: 위험 요소의 규칙과 배치

**Files:**
- Create: `expedition/level/hazards.gd`
- Modify: `data/content/floor_themes.json` (장식에 위험 지형, `hazards` 개수)
- Modify: `data/content/combat.json` (`move_cost`)
- Modify: `expedition/level/floor_generator.gd` (`attempt_layout`, `empty_layout`, `place_features`의 빈 칸 고르기)
- Modify: `expedition/level/continuous_floor.gd` (`apply`의 칸 초기값과 위험 요소 복사, `observe`의 시야)
- Modify: `expedition/time/scheduler.gd` (`environment_tick`)
- Modify: `expedition/run/session.gd` (`conductive`)
- Modify: `expedition/actors/npc_roster.gd` (NPC 자리)
- Test: `tests/hazards.gd` (신규)

**Interfaces:**
- Consumes: Task 2의 파싱 결과 `hazards`, Task 3의 테마
- Produces: `Hazards.TERRAINS := ["lava","deep_water","bog"]`, `Hazards.initial(terrain) -> Dictionary`(`fire`, `wet`), `Hazards.place(layout, theme)`(레이아웃에 `hazards: {Vector2i: Dictionary}`), `Hazards.tick_cell(s, point, cell)`, `Hazards.explode(s, origin)`, `Hazards.sight_radius(s, actor, base: float) -> float`, 상수 `COLLAPSE_RANGE`, `COLLAPSE_DAMAGE`, `GAS_RADIUS`, `GAS_DAMAGE`, `GAS_FIRE`, `BOG_POISON_TICKS`, `FOG_SIGHT_PENALTY`. 계획 2/4의 박쥐 계열 패시브(안개 무시)는 `sight_radius`에 조건 하나를 더하는 식으로 붙인다.

규칙(스펙 §1):
- 용암: 환경 틱마다 불 100으로 되돌린다. 기존 불 투사가 그 위의 대상에게 불 피해를 준다(틱당 최대 20).
- 깊은 물: 틱마다 젖음 100. 이동 지연 150. 늘 전도.
- 늪: 틱마다 젖음 100. 서 있는 대상은 중독 300틱. 이동 지연 120.
- 가스: 칸의 불이 0보다 크면 터진다. 반경 2(체비쇼프) 안의 대상에게 화염 피해 16, 물 계열 지형이 아닌 칸의 불 +40. 가스는 사라진다. 이웃 가스는 다음 차례에 이어서 터진다.
- 무너지는 천장: 살아 있는 파티원이 2칸 안에 오면 흔들리기 시작하고 로그에 "천장이 흔들린다"를 남긴다. 다음 환경 틱(100틱 뒤)에 그 칸의 대상에게 피해 12, 칸은 비어 있으면 벽, 누가 서 있으면 잔해가 된다. 여덟 이웃이 모두 벽이 아닌 칸에만 놓아서 방을 끊지 않는다.
- 안개: 관찰자가 안개 칸에 서 있으면 시야 반경 −3(최소 1.5).
- 배치: 템플릿 글리프의 가스·안개는 그대로 옮긴다. 테마의 `hazards` 개수만큼 별도 난수로 더 놓는다. 구역 1 천장 2~3곳, 구역 2 가스 3~5곳, 구역 4 안개 덩어리 2~3개(반경 2). 구역 2의 장식에 용암, 구역 3의 장식에 깊은 물과 늪을 더한다(일반 방의 장식 덩어리). 조사물·아이템·NPC는 위험 지형 위에 놓지 않는다.

- [ ] **Step 1: 실패하는 테스트 쓰기**

`tests/hazards.gd`:

```gdscript
extends SceneTree
## Zone hazards: lava that never cools, deep water and bog that never dry, gas
## that goes up in fire, ceilings that fall a round after they shake, and fog.
const Session = preload("res://expedition/run/session.gd")
const Fixture = preload("res://tests/floor_fixture.gd")
const Hazards = preload("res://expedition/level/hazards.gd")
const Floor = preload("res://expedition/level/continuous_floor.gd")
const Generator = preload("res://expedition/level/floor_generator.gd")
const Templates = preload("res://expedition/level/floor_templates.gd")
const Scheduler = preload("res://expedition/time/scheduler.gd")
const Rules = preload("res://expedition/combat/combat_rules.gd")
var failures := 0
var checks := 0

func check(ok: bool, reason: String) -> void:
	checks += 1
	if not ok: failures += 1; push_error(reason)

func _initialize() -> void: call_deferred("run")

func run() -> void:
	terrains(); gas(); collapse(); fog(); placement()
	print("Hazards: %d checks, %d failures" % [checks,failures]); quit(1 if failures else 0)

## An open arena with the hero in the middle and every monster gone.
func arena() -> Dictionary:
	var s = Session.new_run(731)
	var c: Vector2i = Fixture.arena(s,8)
	for n in s.npcs: n.hp = 0
	s.floor_state.observe(s)
	return {"s":s,"c":c,"hero":s.party[0]}

func terrains() -> void:
	check(Hazards.initial("lava") == {"fire":100,"wet":0} and Hazards.initial("bog") == {"fire":0,"wet":100},"hazard terrains start hot or soaked")
	check(Hazards.initial("water").wet == 70 and Hazards.initial("stone") == {"fire":0,"wet":0},"old terrains start as before")
	var d := arena(); var s = d.s
	var lava: Vector2i = d.c+Vector2i(1,0)
	s.tile(lava).terrain = "lava"; s.tile(lava).fire = 30
	d.hero.pos = lava
	var hp: int = d.hero.hp
	Scheduler.environment_tick(s)
	check(int(s.tile(lava).fire) >= 95,"lava is back at full heat after a tick")
	check(int(d.hero.hp) < hp,"standing in lava burns")
	var deep: Vector2i = d.c+Vector2i(-2,0)
	s.tile(deep).terrain = "deep_water"; s.tile(deep).wet = 0
	d.hero.pos = d.c
	Scheduler.environment_tick(s)
	check(int(s.tile(deep).wet) >= 98,"deep water never dries")
	s.tile(deep).wet = 0
	check(s.conductive(deep),"deep water always carries a charge")
	check(Rules.move_time(s,d.hero,deep) == 150,"wading deep water is slow")
	var bog: Vector2i = d.c+Vector2i(0,2)
	s.tile(bog).terrain = "bog"
	d.hero.pos = bog; d.hero.statuses.erase("poison")
	Scheduler.environment_tick(s)
	check(d.hero.statuses.has("poison"),"standing in bog poisons")
	check(int(s.tile(bog).wet) >= 98,"bog never dries")
	check(Rules.move_time(s,d.hero,bog) == 120,"bog drags the feet")

func gas() -> void:
	var d := arena(); var s = d.s
	var pocket: Vector2i = d.c+Vector2i(2,0)
	s.tile(pocket).gas = true
	Scheduler.environment_tick(s)
	check(bool(s.tile(pocket).get("gas",false)),"a cold gas pocket waits")
	var hp: int = d.hero.hp
	s.tile(pocket).fire = 10
	Scheduler.environment_tick(s)
	check(not s.tile(pocket).has("gas"),"fire sets the pocket off")
	check(hp-int(d.hero.hp) >= Hazards.GAS_DAMAGE,"the blast reaches two cells")
	check(int(s.tile(d.c+Vector2i(3,0)).fire) >= Hazards.GAS_FIRE-10,"the blast sets the ground alight")
	check(s.log_lines.any(func(l): return l.contains("가스 폭발")),"the blast is logged")
	var far := arena(); var t = far.s
	var away: Vector2i = far.c+Vector2i(5,0)
	t.tile(away).gas = true; t.tile(away).fire = 10
	var whole: int = far.hero.hp
	Scheduler.environment_tick(t)
	check(int(far.hero.hp) == whole,"a blast five cells off does not reach")

func collapse() -> void:
	var d := arena(); var s = d.s
	var roof: Vector2i = d.c+Vector2i(2,2)
	s.tile(roof).collapse = {"armed":false,"at":0}
	Scheduler.environment_tick(s)
	check(bool(s.tile(roof).collapse.armed) and int(s.tile(roof).collapse.at) == int(s.time)+100,"the ceiling shakes when the party comes near")
	check(s.log_lines.any(func(l): return l.contains("천장이 흔들린다")),"the shaking is logged")
	var ally_free_hp: int = d.hero.hp
	s.time += 100
	Scheduler.environment_tick(s)
	check(s.tile(roof).terrain == "wall" and not s.tile(roof).has("collapse"),"an empty cell fills with stone")
	check(int(d.hero.hp) == ally_free_hp,"nobody under it, nobody hurt")
	var under := arena(); var u = under.s
	var spot: Vector2i = under.c+Vector2i(1,1)
	u.tile(spot).collapse = {"armed":true,"at":int(u.time)}
	under.hero.pos = spot
	var hp: int = under.hero.hp
	Scheduler.environment_tick(u)
	check(hp-int(under.hero.hp) >= Hazards.COLLAPSE_DAMAGE and u.tile(spot).terrain == "rubble","whoever stands under it is hit and the cell becomes rubble")
	var distant := arena(); var v = distant.s
	var high: Vector2i = distant.c+Vector2i(5,5)
	v.tile(high).collapse = {"armed":false,"at":0}
	Scheduler.environment_tick(v)
	check(not bool(v.tile(high).collapse.armed),"a ceiling far off stays still")

func fog() -> void:
	var d := arena(); var s = d.s
	var seen: Vector2i = d.c+Vector2i(5,0)
	s.floor_state.observe(s)
	check(s.floor_state.visible.has(seen),"five cells off is in sight on clear ground")
	s.tile(d.c).fog = true
	s.floor_state.observe(s)
	check(not s.floor_state.visible.has(seen),"fog cuts the sight radius")
	check(s.floor_state.visible.has(d.c+Vector2i(1,0)),"the next cell is still in sight")
	check(is_equal_approx(Hazards.sight_radius(s,d.hero,6.0),3.0),"three less in fog")
	s.tile(d.c).erase("fog")
	check(is_equal_approx(Hazards.sight_radius(s,d.hero,6.0),6.0),"clear ground keeps the radius")

func placement() -> void:
	check(Templates.parse(Templates.definition("smelter").rows).hazards.size() == 2,"template glyphs carry their hazards")
	var expected := {1:"collapse",4:"gas",10:"fog"}
	for depth in expected:
		var theme: Dictionary = Floor.theme_for(depth)
		for seed_value in range(3):
			var layout: Dictionary = Generator.generate(theme,seed_value,depth)
			var flag: String = expected[depth]
			var cells: Array = layout.hazards.keys().filter(func(p): return layout.hazards[p].has(flag))
			check(not cells.is_empty(),"floor %d places %s (seed %d)" % [depth,flag,seed_value])
			check(cells.all(func(p): return layout.terrain[p.y*layout.size+p.x] != "wall"),"hazards sit on open ground (floor %d)" % depth)
			if flag == "collapse":
				for p in cells:
					var open := true
					for dy in [-1,0,1]:
						for dx in [-1,0,1]:
							if layout.terrain[(p.y+dy)*layout.size+p.x+dx] == "wall": open = false
					check(open,"a ceiling falls only where it cannot cut a room (%s)" % p)
			for p in layout.features:
				check(layout.terrain[p.y*layout.size+p.x] not in Hazards.TERRAINS,"no curio or item on hazard ground (floor %d)" % depth)
	check(range(5).any(func(sd): return Generator.generate(Floor.theme_for(5),sd,5).terrain.has("lava")),"the mines carry lava")
	check(range(5).any(func(sd): return Generator.generate(Floor.theme_for(8),sd,8).terrain.any(func(x): return x in ["deep_water","bog"])),"the temple carries deep water or bog")
	var s = Session.new_run(4)
	s.depth = 4; s.floor_state.build(s)
	var gassy: int = 0
	for cell in s.tiles: if bool(cell.get("gas",false)): gassy += 1
	check(gassy == s.floor_state.layout.hazards.values().filter(func(h): return h.has("gas")).size(),"the floor copies its gas onto the tiles")
	check(s.tiles.all(func(c): return c.terrain != "lava" or int(c.fire) == 100),"lava tiles start at full heat")
	for depth in [5,8]:
		var t = Session.new_run(9)
		t.depth = depth; t.floor_state.build(t); t.NpcRoster.place(t)
		check(t.npcs.filter(func(n): return n.hp > 0).all(func(n): return t.tile(n.pos).terrain not in Hazards.TERRAINS),"no NPC stands on hazard ground (floor %d)" % depth)
```

- [ ] **Step 2: 실패 확인**

Run: `godot --headless --path . --editor --import --quit; godot --headless --path . --script res://tests/hazards.gd`
Expected: FAIL — `hazards.gd`를 불러올 수 없음.

- [ ] **Step 3: `hazards.gd` 쓰기**

`expedition/level/hazards.gd`:

```gdscript
extends RefCounted
## Zone hazards (spec §1). Three terrains keep themselves hot or soaked each
## environment tick; three tile flags (gas, collapse, fog) are placed by the
## generator and copied onto the tiles when a floor is applied.
const TERRAINS := ["lava","deep_water","bog"]
const WATERY := ["water","deep_water","bog"]
const COLLAPSE_RANGE := 2
const COLLAPSE_DAMAGE := 12
const GAS_RADIUS := 2
const GAS_DAMAGE := 16
const GAS_FIRE := 40
const BOG_POISON_TICKS := 300
const FOG_SIGHT_PENALTY := 3.0
const FOG_MIN_SIGHT := 1.5
const FOG_RADIUS := 2
## Separate from the layout generator so packs never shift with hazards.
const RNG_SALT := 0x48415A
const OVERLAY := {"lava":Color(1.0,0.35,0.05,0.55),"deep_water":Color(0.05,0.2,0.5,0.45),"bog":Color(0.3,0.45,0.1,0.5)}
const FOG_COLOR := Color(0.75,0.78,0.85,0.35)
const GAS_COLOR := Color(0.55,0.8,0.2,0.8)
const COLLAPSE_IDLE := Color(0.6,0.5,0.35,0.7)
const COLLAPSE_ARMED := Color("f37575")

static func initial(terrain: String) -> Dictionary:
	match terrain:
		"lava": return {"fire":100,"wet":0}
		"deep_water", "bog": return {"fire":0,"wet":100}
		"water": return {"fire":0,"wet":70}
	return {"fire":0,"wet":0}

## The floor's hazard flags: the templates' own glyphs first, then the theme's
## counts drawn on a generator of their own. Writes `layout.hazards`.
static func place(layout: Dictionary, theme: Dictionary) -> void:
	var hazards: Dictionary = {}
	for room in layout.rooms:
		if room.kind != "template": continue
		var marks: Dictionary = room.parsed.get("hazards",{})
		for p in marks: hazards[room.rect.position-Vector2i.ONE+p] = (marks[p] as Dictionary).duplicate()
	var counts: Dictionary = theme.get("hazards",{})
	var rng := RandomNumberGenerator.new()
	rng.seed = int(layout.seed)+RNG_SALT
	if counts.has("collapse"):
		var rooms: Array = layout.rooms.filter(func(r): return r.kind == "plain")
		scatter(layout,hazards,rooms,rng,counts.collapse,func(p): return fully_open(layout,p),func(p): hazards[p] = {"collapse":{"armed":false,"at":0}})
	if counts.has("gas"):
		var rooms: Array = layout.rooms.filter(func(r): return r.kind in ["plain","fight"])
		scatter(layout,hazards,rooms,rng,counts.gas,func(_p): return true,func(p): hazards[p] = {"gas":true})
	if counts.has("fog"):
		var rooms: Array = layout.rooms.filter(func(r): return r.kind in ["plain","fight"])
		scatter(layout,hazards,rooms,rng,counts.fog,func(_p): return true,func(p): blanket(layout,hazards,p))
	layout.hazards = hazards

## `range` hazards in randomly drawn rooms, each on a cell `allowed` accepts.
static func scatter(layout: Dictionary, hazards: Dictionary, rooms: Array, rng: RandomNumberGenerator, range: Array, allowed: Callable, put: Callable) -> void:
	if rooms.is_empty(): return
	var wanted: int = rng.randi_range(int(range[0]),int(range[1]))
	var guard := 0
	var placed := 0
	while placed < wanted and guard < 60:
		guard += 1
		var room: Dictionary = rooms[rng.randi_range(0,rooms.size()-1)]
		var cells: Array = open_cells(layout,room).filter(func(p): return not hazards.has(p) and allowed.call(p))
		if cells.is_empty(): continue
		put.call(cells[rng.randi_range(0,cells.size()-1)])
		placed += 1

## Ordinary floor in the room: no wall, no hazard terrain, no water, no
## feature, and more than one cell from every door.
static func open_cells(layout: Dictionary, room: Dictionary) -> Array:
	var result: Array = []
	for y in range(room.rect.position.y,room.rect.end.y):
		for x in range(room.rect.position.x,room.rect.end.x):
			var p := Vector2i(x,y)
			var terrain: String = layout.terrain[y*layout.size+x]
			if terrain == "wall" or terrain in TERRAINS or terrain in WATERY or layout.features.has(p): continue
			if room.doors.any(func(d): return maxi(absi(p.x-d.x),absi(p.y-d.y)) <= 1): continue
			result.append(p)
	return result

## Eight open neighbours: a single fallen block here can never cut a room.
static func fully_open(layout: Dictionary, p: Vector2i) -> bool:
	for dy in [-1,0,1]:
		for dx in [-1,0,1]:
			var q: Vector2i = p+Vector2i(dx,dy)
			if q.x < 0 or q.y < 0 or q.x >= layout.size or q.y >= layout.size: return false
			if layout.terrain[q.y*layout.size+q.x] == "wall": return false
	return true

## A bank of fog: every open cell within FOG_RADIUS of the centre.
static func blanket(layout: Dictionary, hazards: Dictionary, centre: Vector2i) -> void:
	for dy in range(-FOG_RADIUS,FOG_RADIUS+1):
		for dx in range(-FOG_RADIUS,FOG_RADIUS+1):
			var p: Vector2i = centre+Vector2i(dx,dy)
			if p.x < 0 or p.y < 0 or p.x >= layout.size or p.y >= layout.size: continue
			if layout.terrain[p.y*layout.size+p.x] == "wall": continue
			var mark: Dictionary = hazards.get(p,{})
			mark["fog"] = true
			hazards[p] = mark

## One environment tick of one tile, before the fire and wetness pass.
static func tick_cell(s, point: Vector2i, cell: Dictionary) -> void:
	match str(cell.terrain):
		"lava": cell.fire = 100
		"deep_water": cell.wet = 100
		"bog":
			cell.wet = 100
			var standing: Dictionary = s.at(point)
			if not standing.is_empty(): s.Statuses.apply(s,standing,"poison",BOG_POISON_TICKS)
	if bool(cell.get("gas",false)) and int(cell.fire) > 0: explode(s,point)
	if cell.has("collapse"): collapse_tick(s,point,cell)

static func explode(s, origin: Vector2i) -> void:
	s.tile(origin).erase("gas")
	s.message("가스 폭발")
	for y in range(origin.y-GAS_RADIUS,origin.y+GAS_RADIUS+1):
		for x in range(origin.x-GAS_RADIUS,origin.x+GAS_RADIUS+1):
			var p := Vector2i(x,y)
			if not s.inside(p) or s.tile(p).terrain == "wall": continue
			var victim: Dictionary = s.at(p)
			if not victim.is_empty(): s.damage(victim,GAS_DAMAGE,999,"FIRE")
			if s.tile(p).terrain not in WATERY: s.tile(p).fire = mini(100,int(s.tile(p).fire)+GAS_FIRE)

static func collapse_tick(s, point: Vector2i, cell: Dictionary) -> void:
	var state: Dictionary = cell.collapse
	if not bool(state.get("armed",false)):
		if s.alive().any(func(a): return maxi(absi(a.pos.x-point.x),absi(a.pos.y-point.y)) <= COLLAPSE_RANGE):
			state.armed = true; state.at = int(s.time)+100
			s.message("천장이 흔들린다")
		return
	if int(s.time) < int(state.at): return
	var victim: Dictionary = s.at(point)
	if not victim.is_empty(): s.damage(victim,COLLAPSE_DAMAGE,999,"IMPACT")
	cell.erase("collapse")
	cell.terrain = "wall" if s.at(point).is_empty() else "rubble"

## The sight radius of `actor`: three less while standing in fog.
static func sight_radius(s, actor: Dictionary, base: float) -> float:
	if actor.is_empty() or not s.inside(actor.pos): return base
	if bool(s.tile(actor.pos).get("fog",false)): return maxf(FOG_MIN_SIGHT,base-FOG_SIGHT_PENALTY)
	return base

## The wash a hazard terrain lays over its floor texture.
static func overlay(cell: Dictionary) -> Color:
	return OVERLAY.get(str(cell.terrain),Color(0,0,0,0))
```

- [ ] **Step 4: 테마와 이동 비용 데이터**

Run:

```bash
python3 - <<'EOF'
import json
p='data/content/floor_themes.json'
d=json.load(open(p,encoding='utf-8'))
t=d['themes']
t['F1_RUINS']['hazards']={"collapse":[2,3]}
t['F2_MINES']['palette']['accents']=["metal","wood","lava"]
t['F2_MINES']['hazards']={"gas":[3,5]}
t['F3_TEMPLE']['palette']['accents']=["water","deep_water","bog"]
t['F3_TEMPLE']['hazards']={}
t['F4_CRYPT']['hazards']={"fog":[2,3]}
json.dump(d,open(p,'w',encoding='utf-8'),ensure_ascii=False,indent=1)
open(p,'a',encoding='utf-8').write('\n')
q='data/content/combat.json'
c=json.load(open(q,encoding='utf-8'))
c['move_cost'].update({"lava":100,"deep_water":150,"bog":120})
json.dump(c,open(q,'w',encoding='utf-8'),ensure_ascii=False,indent=2)
open(q,'a',encoding='utf-8').write('\n')
EOF
git diff --stat data/content/combat.json
```

Expected: `combat.json`은 `move_cost` 세 줄 추가 말고 바뀐 것이 없다. 들여쓰기 차이로 줄이 크게 바뀌면 `git checkout data/content/combat.json` 뒤 `move_cost` 객체에 세 키를 손으로 넣는다.

- [ ] **Step 5: 생성기에 배치 연결**

`expedition/level/floor_generator.gd` 머리에 `const Hazards = preload("res://expedition/level/hazards.gd")`를 넣는다.
- `attempt_layout`의 `var layout := {...}` 사전에 `"hazards":{},`를 더한다(`"npc_rooms":[],` 뒤).
- `attempt_layout`의 `layout.encounters = build_encounters(layout,theme,painted,rng,depth)` 줄 바로 아래에 `Hazards.place(layout,theme)`를 넣는다.
- `empty_layout`의 반환 사전에 `"hazards":{},`를 더한다.
- `place_features`의 `free_cell` 람다 첫 줄을 다음으로 바꾼다(위험 지형 위에 조사물·아이템을 놓지 않는다).

```gdscript
		var cells: Array = floor_cells(layout.terrain,size,room.rect).filter(func(p): return not features.has(p) and layout.terrain[index_of(size,p)] not in Hazards.TERRAINS and room.doors.all(func(d): return maxi(absi(p.x-d.x),absi(p.y-d.y)) > 1))
```

- [ ] **Step 6: 층 적용과 시야**

`expedition/level/continuous_floor.gd` 머리에 `const Hazards = preload("res://expedition/level/hazards.gd")`를 넣는다.

`apply`의 칸 만들기 줄

```gdscript
			s.tiles.append({"terrain":terrain,"source_terrain":terrain,"pillar":layout.get("pillars",{}).has(Vector2i(x,y)),"fire":0,"wet":70 if terrain == "water" else 0,"variant":posmod(x*13+y*7,3),"palette":0})
```

를 다음으로 바꾸고, 두 겹 루프가 끝난 바로 뒤에 위험 요소 복사를 넣는다.

```gdscript
			var start: Dictionary = Hazards.initial(terrain)
			s.tiles.append({"terrain":terrain,"source_terrain":terrain,"pillar":layout.get("pillars",{}).has(Vector2i(x,y)),"fire":int(start.fire),"wet":int(start.wet),"variant":posmod(x*13+y*7,3),"palette":0})
	for p in layout.get("hazards",{}): s.tile(p).merge((layout.hazards[p] as Dictionary).duplicate(true),true)
```

`observe`의 앞부분

```gdscript
	visible.clear()
	var radius := SIGHT_RADIUS
	var before := ceili(radius)
	var side := before*2+1
	var center := observer(s)
```

을 다음으로 바꾼다(가려진 시야 반경만큼만 본다. `before`는 계산 창의 크기라서 그대로 둔다).

```gdscript
	visible.clear()
	var center := observer(s)
	var radius: float = Hazards.sight_radius(s,center,SIGHT_RADIUS)
	var before := ceili(SIGHT_RADIUS)
	var side := before*2+1
```

- [ ] **Step 7: 환경 틱, 전도, NPC 자리**

`expedition/time/scheduler.gd` 머리에 `const Hazards = preload("res://expedition/level/hazards.gd")`를 넣는다. `environment_tick`의 두 겹 루프 안 `var cell: Dictionary = s.tile(point)` 줄 바로 아래에 넣는다.

```gdscript
			Hazards.tick_cell(s,point,cell)
```

`expedition/run/session.gd`의 `conductive`를 바꾼다.

```gdscript
func conductive(point: Vector2i) -> bool:
	return tile(point).terrain in ["metal", "water", "deep_water"] or tile(point).wet >= 25
```

`expedition/actors/npc_roster.gd` 머리에 `const Hazards = preload("res://expedition/level/hazards.gd")`를 넣고, `place`의 자리 고르기 줄

```gdscript
		var cells: Array = Generator.floor_cells(s.floor_state.layout.terrain,s.floor_state.layout.size,room.rect).filter(func(p): return s.at(p).is_empty() and not s.floor_state.features.has(p))
```

를 다음으로 바꾼다.

```gdscript
		var cells: Array = Generator.floor_cells(s.floor_state.layout.terrain,s.floor_state.layout.size,room.rect).filter(func(p): return s.at(p).is_empty() and not s.floor_state.features.has(p) and s.tile(p).terrain not in Hazards.TERRAINS)
```

- [ ] **Step 8: 통과 확인**

Run: `godot --headless --path . --editor --import --quit; for t in hazards zones floor_generator floor_descent boss_floor continuous_floor npc_roster curios solo_floor playthrough; do godot --headless --path . --script "res://tests/$t.gd" 2>&1 | tail -1; done`
Expected: 모두 0 failures. `floor_generator`의 레이아웃 키 검사가 `hazards` 키 때문에 깨지면 그 검사의 키 목록에 `"hazards"`를 더한다.

- [ ] **Step 9: 커밋**

```bash
git commit -m "Add zone hazards: lava, deep water, bog, gas, falling ceilings and fog

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>" -- expedition/level/hazards.gd data/content/floor_themes.json data/content/combat.json expedition/level/floor_generator.gd expedition/level/continuous_floor.gd expedition/time/scheduler.gd expedition/run/session.gd expedition/actors/npc_roster.gd tests/hazards.gd
```

(Step 8에서 고친 기존 테스트가 있으면 경로를 더한다.)

---

### Task 6: 새 테마와 위험 요소 그리기

**Files:**
- Modify: `expedition/ui/board.gd` (`paint_terrain`, 칸 표시 루프, `uses_pixel_floor_art`)
- Modify: `expedition/art/floor1_art.gd` (`tile`, `terrain`)
- Modify: `expedition/art/mobile_art.gd` (`terrain`)
- Test: `tests/hazards.gd` (함수 추가)

**Interfaces:**
- Consumes: `Hazards.overlay`, `Hazards.FOG_COLOR`, `GAS_COLOR`, `COLLAPSE_IDLE`, `COLLAPSE_ARMED`
- Produces: 신전은 폐허 판석을, 묘역은 폐광 판석을 쓰고 테마 색조를 입힌다(전용 그림은 이후 아트 작업). 용암·깊은 물·늪은 기존 재질 위에 색을 덮는다. 가스는 녹색 원, 안개는 회색 막, 천장은 잔금 테두리(흔들리면 붉은 테두리와 "!")로 그린다.

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/hazards.gd`의 머리에 `const Art = preload("res://expedition/art/mobile_art.gd")`를 넣고, `run()`의 `placement()` 다음에 `await drawing()`을 넣은 뒤(`run`이 `await`를 쓰므로 그대로 둔다) 파일 끝에 붙인다.

```gdscript
func drawing() -> void:
	check(Hazards.overlay({"terrain":"lava"}).a > 0 and Hazards.overlay({"terrain":"bog"}).a > 0 and Hazards.overlay({"terrain":"stone"}).a == 0,"hazard ground is washed with its colour")
	check(Art.terrain({"terrain":"lava"},Vector2i.ZERO,"F3_TEMPLE") != null and Art.terrain({"terrain":"deep_water"},Vector2i.ZERO,"F4_CRYPT") != null,"the new themes and hazard ground have textures")
	var scene = load("res://expedition/ui/main.tscn").instantiate()
	var s = Session.new_run(4)
	s.depth = 4; s.floor_state.build(s)
	var c: Vector2i = s.party[0].pos
	s.tile(c+Vector2i(1,0)).terrain = "lava"; s.tile(c+Vector2i(0,1)).gas = true
	s.tile(c+Vector2i(1,1)).collapse = {"armed":true,"at":int(s.time)+100}; s.tile(c).fog = true
	s.floor_state.observe(s)
	scene.session = s; root.size = Vector2i(390,844); root.add_child(scene)
	for frame in range(4): await process_frame
	check(scene.map_view != null,"the board draws a floor with every hazard on it")
	scene.queue_free()
```

`run()`를 `func run() -> void:` 그대로 두고 첫 줄을 `terrains(); gas(); collapse(); fog(); placement(); await drawing()`로 바꾼다.

- [ ] **Step 2: 실패 확인**

Run: `godot --headless --path . --script res://tests/hazards.gd`
Expected: FAIL — `the new themes and hazard ground have textures`(지금은 `F3_TEMPLE`이 픽셀 그림 경로에 없다) 또는 그리기 중 `SCRIPT ERROR`.

- [ ] **Step 3: 그림 재질**

`expedition/art/floor1_art.gd`의 `tile`에서 폐광 시트를 고르는 세 조건 `theme_id == "F2_MINES"`를 모두 `theme_id in ["F2_MINES","F4_CRYPT"]`로 바꾼다. `terrain`의 재질 사전을 바꾼다.

```gdscript
	var id: String = {"wood":"wood","water":"water","metal":"metal","wall":"front","rubble":"rubble","dirt":"dirt",
		"lava":"rubble","deep_water":"water","bog":"water"}.get(cell.terrain,["floor_a","floor_b","floor_c","floor_d"][posmod(point.x*7+point.y*3,4)])
```

`expedition/art/mobile_art.gd`의 `terrain` 첫 줄 `if theme_id in ["F1_RUINS","F2_MINES"]: return FirstFloor.terrain(cell,point,theme_id)`를 `if theme_id in ["F1_RUINS","F2_MINES","F3_TEMPLE","F4_CRYPT"]: return FirstFloor.terrain(cell,point,theme_id)`로 바꾼다.

- [ ] **Step 4: 판 그리기**

`expedition/ui/board.gd` 머리의 상수 목록에 넣는다.

```gdscript
const Hazards = preload("res://expedition/level/hazards.gd")
## The two zones without art of their own yet borrow the ruins and the mines
## under a tint of their own.
const THEME_TINT := {"F3_TEMPLE":Color(0.82,0.95,1.0),"F4_CRYPT":Color(0.86,0.8,0.95)}
```

`uses_pixel_floor_art`를 바꾼다.

```gdscript
func uses_pixel_floor_art() -> bool:
	return session.floor_state.theme_id in ["F1_RUINS","F2_MINES","F3_TEMPLE","F4_CRYPT"]
```

`paint_terrain`의 `var tint := MEMORY_TINT if visibility == 1 else Color.WHITE` 줄 아래에 `tint *= THEME_TINT.get(session.floor_state.theme_id,Color.WHITE)`를 넣고, 벽이 아닌 칸의 `else:` 가지에서 `Art.Masonry.paint_floor_shadow(self,rect,point,is_wall_tile)` 줄 바로 위에 넣는다.

```gdscript
				if visibility != 1:
					var wash: Color = Hazards.overlay(cell)
					if wash.a > 0: draw_rect(rect,wash)
					if bool(cell.get("fog",false)): draw_rect(rect,Hazards.FOG_COLOR)
```

칸 표시 루프의 `if cell.wet > 0 and cell.terrain != "water": outline(polygon,Color(0.3,0.6,0.8,0.6))` 줄을 다음으로 바꾼다.

```gdscript
			if cell.wet > 0 and cell.terrain not in Hazards.WATERY: outline(polygon,Color(0.3,0.6,0.8,0.6))
			if bool(cell.get("gas",false)): draw_circle(center,half_width*0.3,Hazards.GAS_COLOR)
			if cell.has("collapse"):
				var armed: bool = bool(cell.collapse.get("armed",false))
				outline(polygon,Hazards.COLLAPSE_ARMED if armed else Hazards.COLLAPSE_IDLE,3 if armed else 1)
				if armed: draw_string(ui_font,center+Vector2(-4,4),"!",HORIZONTAL_ALIGNMENT_LEFT,-1,18,Color.WHITE)
```

- [ ] **Step 5: 통과 확인**

Run: `godot --headless --path . --editor --import --quit; for t in hazards mobile_hud ui_smoke battle_presentation; do godot --headless --path . --script "res://tests/$t.gd" 2>&1 | tail -1; done`
Expected: `hazards`, `mobile_hud`, `battle_presentation` 0 failures. `ui_smoke`가 이 계획 전부터 깨져 있었다면(이전 세션에서 확인됨) 같은 실패인지 `git stash`를 쓰지 않고 `git worktree add`로 만든 깨끗한 작업 트리에서 확인하고, 같으면 무관한 실패로 적어 둔다.

- [ ] **Step 6: 커밋**

```bash
git commit -m "Draw the temple and crypt themes and every zone hazard

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>" -- expedition/ui/board.gd expedition/art/floor1_art.gd expedition/art/mobile_art.gd tests/hazards.gd
```

---

### Task 7: 12층 승리

**Files:**
- Modify: `expedition/run/session.gd` (`victory`, 보스 처치 분기)
- Modify: `expedition/run/descent.gd` (`descend`)
- Modify: `expedition/time/scheduler.gd` (시간 진행 멈춤 두 곳)
- Modify: `expedition/ui/main.gd`, `expedition/ui/screens/result_card.gd`
- Test: `tests/zones.gd` (함수 추가)

**Interfaces:**
- Consumes: `Zones.FINAL_DEPTH`, `Zones.zone_name`
- Produces: `Session.victory()`(단계 `"VICTORY"`, 점수 +500, 로그 한 줄, 여러 번 불러도 한 번). 12층에서 보스가 쓰러지면 세션이 스스로 부른다. 계획 4/4가 최종 보스 처치에서 다시 불러도 한 번만 적용된다. 12층에서는 `descend()`가 거절한다.

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/zones.gd`의 `run()`을 `func run() -> void:` 그대로 두고 마지막 `print` 줄 바로 위에 `victory()`와 `await victory_card()`를 넣은 뒤 파일 끝에 붙인다.

```gdscript
func victory() -> void:
	var s = Session.new_run(731)
	s.depth = 12; s.floor_state.build(s)
	var boss: Dictionary = s.enemies.filter(func(e): return e.get("boss",false))[0]
	for e in s.enemies: if not e.get("boss",false): e.hp = 0
	var score: int = s.score
	s.damage(boss,99999,int(s.party[0].id),"physical")
	check(boss.hp <= 0 and s.phase == "VICTORY","the twelfth floor's boss ends the run in victory")
	check(s.score >= score+500,"victory is worth five hundred")
	var after: int = s.score
	s.victory()
	check(s.phase == "VICTORY" and s.score == after,"victory counts once")
	check(s.log_lines.any(func(l): return l.contains("원정 성공")),"victory is logged")
	check(not s.on_floor(),"the run is off the floor once it is won")
	var t = Session.new_run(731)
	t.depth = 12; t.floor_state.build(t)
	for e in t.enemies: e.hp = 0
	t.phase = "EXPLORE"
	var stairs: Vector2i = t.floor_state.layout.stairs
	t.party[0].pos = stairs+Vector2i.RIGHT if t.is_free(stairs+Vector2i.RIGHT) else stairs+Vector2i.LEFT
	t.floor_state.observe(t)
	check(not t.descend() and t.depth == 12,"there is no thirteenth floor")
	var u = Session.new_run(731)
	u.depth = 9; u.floor_state.build(u)
	var ninth: Dictionary = u.enemies.filter(func(e): return e.get("boss",false))[0]
	u.damage(ninth,99999,int(u.party[0].id),"physical")
	check(u.phase != "VICTORY","a zone boss before the last is no victory")

func victory_card() -> void:
	var scene = load("res://expedition/ui/main.tscn").instantiate()
	var s = Session.new_run(731)
	s.depth = 12; s.victory()
	scene.session = s; root.size = Vector2i(390,844); root.add_child(scene)
	for frame in range(3): await process_frame
	scene.refresh()
	for frame in range(2): await process_frame
	var card: Node = scene.find_child("ResultCard",true,false)
	check(card != null,"victory shows the result card")
	check(card != null and card.find_children("*","Label",true,false).any(func(l): return l.text.begins_with("승리")),"the card says 승리")
	scene.queue_free()
```

- [ ] **Step 2: 실패 확인**

Run: `godot --headless --path . --script res://tests/zones.gd`
Expected: FAIL — `victory` 메서드 없음.

- [ ] **Step 3: 세션과 하강**

`expedition/run/session.gd` 상수 목록에 `const Zones = preload("res://expedition/level/zones.gd")`를 넣는다. `func check_battle_end() -> void:` 바로 위에 둔다.

```gdscript
## The run is won: the last floor's boss is down. Calling it again changes nothing.
func victory() -> void:
	if phase == "VICTORY": return
	phase = "VICTORY"; score += 500
	message("원정 성공 · %s의 주인을 쓰러뜨렸다" % Zones.zone_name(Zones.zone_of(depth)))
```

`after_damage`의 보스 처치 분기

```gdscript
		if target.get("boss",false):
			if party_hunted:
				grant_part(str(target.part_id)); score += 100
```

바로 아래(같은 `if target.get("boss",false):` 안)에 넣는다.

```gdscript
			if depth >= Zones.FINAL_DEPTH: victory()
```

`expedition/run/descent.gd` 머리에 `const Zones = preload("res://expedition/level/zones.gd")`를 넣고, `descend`의 첫 줄 바로 아래에 넣는다.

```gdscript
	if s.depth >= Zones.FINAL_DEPTH: return false
```

- [ ] **Step 4: 시간 멈춤과 결과 카드**

`expedition/time/scheduler.gd`의 두 곳 `if s.phase == "DEFEAT": return {}`를 `if s.phase in ["DEFEAT","VICTORY"]: return {}`로 바꾼다.

`expedition/ui/main.gd`의 `if session.phase == "DEFEAT": ResultCard.build_result_card(self); return`을 `if session.phase in ["DEFEAT","VICTORY"]: ResultCard.build_result_card(self); return`으로 바꾼다.

`expedition/ui/screens/result_card.gd`의 머리 주석을 "The end of a run, lost or won: the run's numbers, who walked with the party, and the way back to the kit picker."로 바꾸고, `ui.label(card,"%d층에서 쓰러졌다" % session.depth,24)`를 다음으로 바꾼다.

```gdscript
	var won: bool = session.phase == "VICTORY"
	ui.label(card,"승리 · %d층의 주인을 쓰러뜨렸다" % session.depth if won else "%d층에서 쓰러졌다" % session.depth,24)
```

- [ ] **Step 5: 통과 확인**

Run: `godot --headless --path . --editor --import --quit; for t in zones boss_floor playthrough run_start integration; do godot --headless --path . --script "res://tests/$t.gd" 2>&1 | tail -1; done`
Expected: 모두 0 failures.

- [ ] **Step 6: 커밋**

```bash
git commit -m "Win the run on the twelfth floor's boss

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>" -- expedition/run/session.gd expedition/run/descent.gd expedition/time/scheduler.gd expedition/ui/main.gd expedition/ui/screens/result_card.gd tests/zones.gd
```

---

### Task 8: 전체 스위트

- [ ] **Step 1: 모두 돌리기**

Run:

```bash
godot --headless --path . --editor --import --quit
fails=0; for f in tests/*.gd; do n=$(basename $f .gd); case $n in floor_fixture|model_b_runner|difficulty_gate) continue;; esac; out=$(timeout 600 godot --headless --path . --script "res://tests/$n.gd" 2>&1); code=$?; if [ $code -ne 0 ] || echo "$out" | grep -q "SCRIPT ERROR\|^ERROR:"; then echo "FAIL $n"; fails=$((fails+1)); fi; done; echo "failing suites: $fails"
```

Expected: `failing suites: 0`. 이 계획 전부터 깨져 있던 스위트(`ui_smoke` 등)는 깨끗한 작업 트리(`git worktree add`)에서 같은 실패가 나는지 확인하고, 같으면 무관한 실패로 적어 둔다.

- [ ] **Step 2: 깨진 기존 스위트 고치기**

2층의 테마가 `F1_RUINS`로 바뀌어 기대값이 어긋난 스위트, 레이아웃에 `hazards` 키가 생겨 키 목록을 비교하는 스위트가 남아 있으면 기대값만 새 규칙으로 고친다. 검사를 지우지 않는다. 고친 파일은 이 단계의 커밋에 넣는다.

```bash
git commit -m "Bring the remaining suites to the four-zone floors

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>" -- <이 단계에서 고친 테스트 파일들>
```

---

## 스펙 대응표

| 스펙 | 작업 |
|---|---|
| §0.1 12층 4구역, 3·6·9·12층 보스, 12층 승리 | Task 1, 3, 7 |
| §1 구역 표(장소, 주 속성, 고유 위험, 보스 방) | Task 1(표), 3(테마), 5(위험), 2(보스 방 배치) |
| §1 테마: F1·F2 계승, F3·F4 신규, `(depth−1)/3`으로 구역 | Task 3 |
| §1 층의 주 속성, 구역 1의 1·2층 없음·3층 독 | Task 1, 4 |
| §1 전용 방 템플릿 두세 개씩 | Task 2(구역마다 둘, 기존 방 공유), 3(싸움 방 목록) |
| §1 고유 위험 여섯 | Task 5(규칙과 배치), 6(그리기) |
| §1 재생성 없음 | 변경 없음(위험 요소가 몬스터를 만들지 않는다) |
| §1 승리 결과 카드 | Task 7 |
| §3.2 깊은 층 보정 삭제 | Task 3 |
| §7 보스 방 네 개(배치만) | Task 2, 3. 보스 행동과 레버·열기 등은 계획 4/4 |
| §8 테마, 템플릿, 몬스터 뽑기, 변종, 결과, 그리기 행 | Task 2~7 |
| §9 `tests/zones.gd` | Task 1, 3, 4, 7 |
| 종족 `zone` 필드와 30종 | 계획 2/4. 이 계획은 `zone`이 없을 때의 대체 규칙(Global Constraints)으로 동작한다 |
| 증기·얼음 칸·독 웅덩이 | 계획 3/4 |
